# Round log — n = 6 minimal superpermutation length

One line of history per research round, oldest first. This is the **chronological**
view; the **logical** account of what is actually proved lives in
[`research/SUPERPERMUTATION_N6_MASTER_STATUS.md`](research/SUPERPERMUTATION_N6_MASTER_STATUS.md)
and the headline claims in [`STATUS.md`](STATUS.md).

> **This project has not proved `L₆ ≥ 872`.** Unconditionally established: `867 ≤ L₆ ≤ 872`
> — the upper bound by an explicit witness verified in this repo, the lower bound cited from
> Houston/Pantone/Vatter (arXiv:1408.5108) and not re-derived here.

**Evidence tags:** `HP` hand proof · `EC` exhaustive computation · `ER` exact replay ·
`IV` independently verified · `BO` bounded observation (never proof) · `RF` refuted ·
`OPEN` unresolved.

---

## How to update this file

Append one row per round at the **bottom** of the table below, then update the
*Current ledger* block. A row is not complete without: the round number, a one-sentence
headline that states the **outcome** (not the intention), the deliverable path, and the
commit. Retractions get their own row **and** a strikethrough edit of the row they retract —
never a silent deletion. Rounds that produced no closure are recorded exactly like rounds
that did; the negative results are the reason the positive ones are trustworthy.

---

## Current ledger (update every round)

| | |
|---|---|
| Proof-valid Q2 / Area-A residual | **4,802** states *(… − 196 R94 Layer A − 32 R94 B2-only, **Codex-audited**)* |
| Canonical residual classes | not recomputed since Round 79 *(761 was Claude-computed, not audited)* |
| Roots with empty Q2 residual | **28 / 33** (every long root clear) |
| Q2-admissible frontier (Round 71) | 3,248,890 |
| Last round | **105** (pure falsification) — one attack **succeeded**: the `L2 = c − 1` bound is **false for general 0/1 digraphs** (111 brute-force counterexamples); it silently needs **cost-0 out-degree ≤ 1**. That precondition holds on **all 173,409** real assignments (max degree 1, 0 violations) and is now asserted, so the Round-104 counts survive. `ΔF = 0` likewise needed sharpening — `ℓ<5` abandons generically, but fragment repair verifies **5,732/5,732**. 10,000 synthetic positive controls: **false UNSAT 0**. **But the collapse is fragile: 3,359 pairs close with margin exactly 1.** Audited ledger stays **4,782** |
| Unconditional bounds | `867 ≤ L₆ ≤ 872` |

---

## Phase I — foundations and the J branch (rounds 1–20)

| round | outcome | artifact / commit |
|---|---|---|
| 1–9 | E-orbit and hexagon foundations laid; J-branch discovered; capacity-obstruction proof | `0626f9c`..`d72520b` |
| 10–20 | Unique weight-2 move proof; RR same-component ⟺ chaining; Unique Hub Hexagon lemma; Hub Touch Count ≤ 2; corpus-completeness correction | `9b754f4`..`4c8b8ad` |

## Phase II — the parity detour (rounds 21–28)

| round | outcome | artifact / commit |
|---|---|---|
| 21–28 | Preparation-parity conjecture proposed and then **refuted** `[RF]`; corrected identity kept. Round 27's own `k ≥ 1` reading was itself wrong and was corrected to `k = 0` in Round 28 | `e775b1b`..`4d9bdc7`, `research/RR_PARITY_CONJECTURE_REFUTATION.md` |

## Phase III — Target A / Target B (rounds 29–38)

| round | outcome | artifact / commit |
|---|---|---|
| 29–34 | Terminal normal form; Target B defined; the known-18 Target-A ledger closed 18→9→8→7→**0** | `129d73a`..`d664019` |
| 35–38 | Target-A search rebuilt, finding **+1,398** new boundaries; capacity-helper firewall erected; five short roots identified as still open | `d07d267`..`232718e`, `aeafd1c`..`9b345c4` |

## Phase IV — four search bugs, found and fixed (rounds 40–54)

Every one of these invalidated a previously reported number. They are listed because the
corrected ledger is only meaningful alongside them.

| round | outcome | artifact / commit |
|---|---|---|
| 40 | **v1 R1-completeness bug** `[RF]`: R-kind macro edges were all treated as terminal; bare short roots must enqueue the first R edge. Fixed | `abfcdca`, `d8600b9`, `e9ff19c` |
| 41–42 | **v2 `O > 25` prune bug** `[RF]`: `O ≤ 25` is a Target-B coordinate and was wrongly applied before the Target-A test, discarding legal states. Fixed | `d90b69a`, `785ddab` |
| 43–48 | v3 taxonomy; **hierarchy macro-entry bug** `[RF]` found and fixed — 38,405 false positives corrected | `24002fd`..`b09f1d5` |
| 49–51 | Corrected short-root ledger; v5 fair pilot; 439-child ledger; top-8 selection | `1f3d11a`..`dfc314f` |
| 52–54 | v6 endpoint frozen; **v6/v7 provenance-loss bug** `[RF]`; frontier narrows to `short_ell2_r1_37` | `06dae7c`..`fae8ded` |

## Phase V — the T4 program (rounds 55–61)

| round | outcome | artifact / commit |
|---|---|---|
| 55–56 | All-13 pilot: 7 closed, 6 survive, 84-state frontier | `c394624`, `e280d32` |
| 57 | Dangerous-entry realizability audit: 196 mechanisms, T1-level | `6811132` |
| 58 | Stage D: full first-component-Z3 search over 6 checkpoints, 1,325,392 nodes | `9342018` |
| 59 | FZ1 provenance audit; **144-Z3 bound proof method retracted** `[RF]` (the numeric value is not disproven, only the method) | `bb3e9e1` |
| 60 | C4 collision obstruction: T2 (253,537 collisions), T2a four-hexagon closure | `2b3fb8f` |
| 61 | Hex-82 five-route closure (T2b); T4 asserted — still the strongest single result in the program | `19d484b`, `1f9efff` |
| 61 | **Independent T4 verification** (`CLAUDE_T4_VERIFIED`); generic VNTS theory | `3f24a49`, `f7a7211` |

## Phase VI — independent verification track (rounds 62–)

From here the work is the independent verification track: every Codex-produced datum is
re-derived from raw per-record data before use, and each round ends in a single verdict token.

| round | outcome | artifact |
|---|---|---|
| 62–68 | Master status document; G3 residual theory; Ω-projection soundness / monotonicity / termination; 1,818-anchor corpus analysis; the `short_ell1_r1_94:frontier:76` singleton resolved as generic | `2730c99`..`3370be4` |
| 69 | **Φ / unique-bridge invariant `6r ≤ 11 − Φ`** ⟹ `r ≤ 1`; σ-adjacency admissibility lemma; 1,415 of 1,818 residual anchors closed | `research/RR_SHORT_G3_COCOMPONENT_INVARIANT_CLAUDE.md` |
| 69b | ELL4 unique-bridge Target-A normal form: the remaining 403 `root_ell = 4` anchors closed; 0 new Target-A classes | `research/RR_SHORT_ELL4_UNIQUE_BRIDGE_NORMAL_FORM_CLAUDE.md` |
| 70 | All 1,398 Rounds-35–37 Target-A boundaries reclassified: only 6 are Q2-admissible and they are exactly the known-18; Target B closed **1,398/1,398**; 0 survivors. *(Self-caught error: a "Φ-free" claim in this round was wrong and was corrected in place in Round 71 — the totals were unaffected.)* | `research/RR_TARGET_A_1398_RECLASSIFICATION_CLAUDE.md` |
| 71 | Q2/Area-A proof frontier rebuilt. The 33 coverage searches **were never exhausted** (3,321,753 states left queued). Q2-admissible 3,248,890 → **residual 200,408** in 1,570 classes; the real gap is boundary-list incompleteness, not a surviving mechanism | `research/RR_Q2_AREA_A_PROOF_FRONTIER_CLAUDE.md` |
| 72 | ~~SKIP-COST closes 114,298~~ — **RETRACTED in Round 73**. The `E¹`/`E²` engine facts survive | `research/RR_SKIP_COST_THEOREM_CLAUDE.md` |
| 73 | **SKIP-COST retracted** after Codex's audit, independently witnessed with my own `q0`-return walk: the evaluator omitted `q0` re-entry and repeated re-entry, so a supply-side upper bound under-estimated. 95,225 closures void. Repaired residual **200,408** — I did not adopt Codex's 273,125, which discards the sound demand-side orbit-re-entry closure of 72,717 | `research/RR_Q2_LEDGER_REPAIR_CLAUDE.md` |
| 74 | TOTAL RE-ENTRY LOWER BOUND: **sound but vacuous**. `seg_max(q) = |live(q)|` for all 32 masks, so the bound degenerates to Round-71 ORBIT-REENTRY with +1 on both sides. Closure **0**. Retires the per-orbit segment-count family | `research/RR_TOTAL_REENTRY_PAYOFF_TEST_CLAUDE.md` |
| 75 | Inter-orbit sequencing: the orbit-only graph is **insufficient** (0 of 7,920 transitions are phase-universal), but the required `(orbit, phase)` refinement is strongly connected → crossings 0, closure **0**. Near-miss recorded: a free-movement graph omitting `E¹` showed 15 SCCs and would have produced an unsound bound | `research/RR_ORBIT_SEQUENCING_CLAUDE.md` |
| 76 | BRIDGE-CHARGE: **refuted**. `r = 0` covers 196,056 of 200,408 (97.83 %), so the candidate had real leverage — but `E¹` creates the first bridge with `ΔΦ = ΔNdef = ΔO = ΔF = 0`, witnessed on corpus and by an independent 3-edge walk from `initial_state`. Closure **0** | `research/RR_BRIDGE_CHARGE_CLAUDE.md` |
| 77 | **ORBIT-HEXAGON COVER — major payoff.** Quotienting out `E¹` first: its closure is bounded (≤ 4 steps on `q0`'s phase 5-cycle) and it freely repairs `D`, `P` and re-entry demand — which is exactly why rounds 74–76 died — but leaves `O`, coverage and incidence collisions strictly invariant. New theorem: `COLLISIONS = 5·O − |covered| ≤ 5`, monotone and demand-side. Closes **122,194 of 200,408 (60.97 %)** → residual **78,214** in **1,312** classes, 25/33 roots clear | `research/RR_ORBIT_HEXAGON_COVER_CLAUDE.md` || 78 | **EXACT COVER at `COLLISIONS = 5` — major payoff.** With the collision budget spent, the `K = 25 − O` orbits still to open must exactly cover the uncovered hexagon set; `|U| = 5K` verified with 0 failures on all 33,564. Coverability closes 24,419, forced-orbit conflict 5,315, complete Algorithm X 3,682 — **33,416 of 33,564 (99.56 %)**, with **0** node-cap UNKNOWNs and all 3,570 search-UNSATs re-decided under a different variable order (0 disagreements). 148 SAT survivors, **none with a unique cover** (min 6 covers). Residual **44,798** in **1,050** classes, 26/33 roots clear | `research/RR_EXACT_COVER_COLLISION5_CLAUDE.md` || 79 | **SLACK-COVER — major payoff.** Derived (not assumed) that the future excess is **exactly `b = 5 − c`, forced**, the moment `U` is covered — so "excess ≤ b" is the counting slack, not a constraint; what restricts is *K closed orbits must cover U*, with every block wasting ≤ b. Reduces to Round 78 at `b = 0`. Closes **38,141 of 44,650 (85.42 %)** with **0** UNKNOWNs, validated by 620 synthetic guaranteed-SAT instances (0 failures), 8,141 UNSATs re-decided under a different variable order (0 disagreements) and 6,013 verified SAT witnesses. Residual **6,657** in **761** classes, 28/33 roots clear. Power decays 97.4 % → 87.7 % → 40.1 % → **0 %** as slack grows: the static condition is saturated | `research/RR_SLACK_COVER_CLAUDE.md` || 80 | **Artifact repair, no new theorem.** Round 79 preserved only aggregate counters, so Codex could not reproduce 38,141 / 6,657. Exported the full audit archive to `outputs/rr_slack_cover_archive/`: the 44,650-state input ledger (stable sha256 ids, root+index provenance, `c`/`b`/`O`/`K`, `C`/`U`/open-orbit masks), all **43,643** distinct instances **including the 37,630 UNSAT**, with candidate blocks and an explicit `K`-block witness for every SAT, the 148 `c=5` survivors, a pinned incidence table and a schema note. Re-derivation matched Round 79 with **0 mismatches and 0 duplicate ids**, and a **stdlib-only verifier** replays every band and the 6,657 total from the artifact alone. Round 79 now carries: theorem SOUND / counts CLAUDE-REPRODUCED / **independent count audit PENDING** | `outputs/rr_slack_cover_archive/SCHEMA.md`, `src/verify_rr_slack_cover_archive.py` || 81 | **Cover-compatible orbit ordering — no payoff, and a located wall.** Built a maximally conservative opening relation (all 5 phases x 6 rotation lengths x 4 joints; every excluded pair excluded by exhaustion over the group action, not by search absence; `w2:10` deliberately kept since a weight-2 joint into a fresh orbit is a legal engine transition). It reproduces Round 75's 7,920 pairs, out-degree 55. **Closure 0 of 6,657**, 0 UNKNOWN: the reachable fixpoint contains *every* cover-compatible candidate in *every* state, so stage D is provably identical to Round 79 and was not attempted. Two measured reasons: SLACK-COVER leaves a median **128 of 144** orbits individually cover-compatible, and the relation has 38.5 % density. **Negative structural result: static incidence + orbit-level ordering is exhausted.** The wall is located — the phase-refined relation has out-degree **17** and a pinned walk could open as few as **0** candidates, but `E¹` moves phase for free, so port-level occupancy is the only lever that could pin it | `research/RR_COVER_ORDER_CLAUDE.md` || 82 | **Port occupancy pins the phase, and it still closes nothing — with a measured ceiling.** Literal `E¹`/`E²` availability from the engine's own no-repeat predicates: **448 states cannot move phase at all**, the maximal `E¹` chain is **4** (Round-77 cross-check, identical with and without the Area-A prune), and 45 states have `E¹` blocked but `E²` available. From a pinned phase closure only **4–54** of ~130 candidate orbits are openable — a real restriction. **Closure 0 of 6,657, 0 UNKNOWN.** The decisive number: under a deliberately *unsound* strengthening forbidding all phase repair, the first-open test would close **13 of 6,657 (0.20 %)**; with the true closure, **1**. So `E¹` costs this direction 12 states and the direction was worth ≤ 13 throughout. Cause is not `E¹` but the predicate: a first-open test closes only if *every* openable orbit is cover-incompatible, and ~90 % of orbits are individually cover-compatible. **First-open tests are exhausted at every refinement level** | `research/RR_PORT_OCCUPANCY_CLAUDE.md` || 83 | **Fragment repair is not the bottleneck — and the blocked-w2 lemma is now proved.** All 6,657 residual states have `F = 1`, so no further abandonment is legal and **no new fragment can be created**; every remaining opening must be `Z3` at `ΔNdef = 0`, and all 144 orbits admit one. `M_def = 0` everywhere, so the payoff gate stopped the round: **closure 0**. The real product is foundational: `N_exceeded_monotone` is Q1-SAFE and used by every search, and rested on a blocked-w2 lemma the repo cited from prior work and recorded as *"a bounded empirical check, not a proof"*. Exhaustively, `t = E(σ(p'))` — the w2 target shares the blocking window's orbit one phase on — and no-repeat forces a visited blocker to have been a *registered* joint target, so the fresh-orbit case is impossible. Hence `ΔNdef ≥ 0` and the prune is **sound**. Pinned by `tests/test_blocked_w2_lemma.py` | `research/RR_FRAGMENT_REPAIR_CLAUDE.md` || 84 | **Z3-only generation of the final orbit set — orbit level exhausted.** The Z3-only opening relation turns out to be **identical** to Round 81's generic 4-joint relation (7,920 edges, out-degree 55, verified as per-orbit *set equality*; the w2 edges are a strict subset), so the newly proved "only Z3 openings are legal" is invisible at orbit level. Proved the generation-necessity equivalence: `S` is openable in order iff every member is reachable from `A` **inside `G[A ∪ S]`** — strictly stronger than full-graph reachability, and untested before. Stage B closed 0 (the Z3 closure reaches every candidate in every state); 6,181 states admit a cover inside the one-step image; the exact joint solve on the remaining **476 returned 476 SAT, 0 UNSAT, 0 UNKNOWN** — and examined exactly 476 cover solutions, i.e. **the first cover found was induced-reachable every time**. Also derived and recorded a sound lemma — at most two future edges can have `ℓ < 5` — whose `ℓ=5` relation is 5.5× sparser (out-degree 10) yet still reaches every candidate, so it closes 0 too | `research/RR_Z3_GENERATION_CLAUDE.md` || 85 | **2-예외 예산 — 궤도 수준 첫 소득, 13개 폐쇄.** 라운드 84의 보조정리를 범위까지 확정해 재증명: `F=1` ⟹ abandonment 불가 ⟹ 새 육각형에 든 pass 는 반드시 채우고 `ℓ=5` 로 떠남 ⟹ 새 부분육각형이 생기지 않으므로 `ℓ<5` 는 현재 pass 와 fragment 수리뿐, **최대 2개 — 모든 매크로 간선에 대해**(opening 에만 쓰는 것은 안전한 완화). `G5`(ℓ=5) 는 1,440간선·out-degree 10 으로 전체 Z3(7,920·55)보다 5.5배 성기고, `Gshort` 는 전체 Z3 와 동일. 예외 0회 진단은 261개만 실패시키고, 건전한 ≤2 판정은 **248 SAT / 13 UNSAT / 0 UNKNOWN**. 최소 예외 분포 `{0: 6396, 1: 147, 2: 101, ≥3: 13}`. 13개는 cover 해가 1~4개뿐인 극단 인스턴스이며, 독립 재구현과 비-MRV 순서 재판정에서 불일치 0. 잔여 **6,644** | `research/RR_TWO_EXCEPTION_CLAUDE.md` || 86 | **착지 위상은 정확하지만 `E¹` 이 즉시 지운다 — 폐쇄 0, 천장 18.** `(궤도,위상)` 720노드 표: `G5_phase` out-degree **2**(1,440간선), `Gshort_phase` 15. 엔진 확인 — Z3 opening 직후 endpoint = joint target, 착지 port = 등록 port, **위상 자유도 0**. 그러나 `ℓ=5` 같은-궤도 이동은 `E¹`(+1, 720/720, 비용 0)과 `E²`(+2)뿐이고 **`E¹` 단독으로 5-주기 전체 생성** → 건전한 과대근사에서 위상 상관 완전 소실. 2단계 연쇄는 수리 금지 시 **80 % 감소**(14,400→2,880)하지만 건전한 수리 포함 시 0 %. 적대적 천장(수리 전면 금지, UNSOUND)조차 **18개(0.27 %)** 만 닫는다 — 전역 occupancy 를 넣어도 이 방향 상한이 그것. **Codex 정정 2건 수용**: 보조정리 증명을 `P = 120 + F = 121` 전역 pass 계수로 보강, 히스토그램 1/2 분할을 231/17 로 정정(내 `solve()` 조기 중단 버그; 13개 폐쇄는 영향 없음) | `research/RR_PHASE_CHAIN_CLAUDE.md` || 87 | **출발 port 는 1회용임을 증명 — 그래도 폐쇄 0.** 매크로 edge 는 항상 등록 port 에서 출발하고(엔진 39/39), no-repeat 로 각 port 는 1회만 endpoint 이므로 **각 port 는 최대 1개 edge 를 발사**한다(재진입·`E¹`/`E²` 모두 복원 불가). 라벨 G5 기하는 조밀하다: port 당 정확히 **2** target, 같은 궤도 두 port 의 target **겹침 0**(1,440쌍), target 당 공급자는 정확히 10궤도 × 각 1 port. 관대한 천장에서 첫 witness cover **5개가 실제로 매칭 불가**(결손 `{1: 4, 4: 1}`) — 즉 Hall 위반이 진짜로 존재한다. 그러나 적대적 검사에서 `ℓ<5` short edge 가 매칭을 우회함을 발견해 건전 기준을 **결손 ≥3** 으로 세웠고(미반영 시 4개를 잘못 닫을 뻔), 모든 cover 를 열거하니 **6,657 전부 SAT**. 원인은 라운드 81 이후 동일 — cover 선택의 자유가 결손 조합을 언제나 회피 | `research/RR_SOURCE_CAPACITY_CLAUDE.md` || 88 | **Universal cover core 는 실재하지만 아무것도 닫지 못한다 — 폐쇄 0.** cover 가족을 완전 열거하니 표본 1,000 중 캡 2,000 도달은 **단 1개**, 635개는 100개 이하. **397개(39.7 %)가 FORCED 궤도를 최소 1개**(최대 17개) 가지며 941개는 FORBIDDEN 이 20개 이상 — 완전 유한 판정(`q` 금지/강제 시 UNSAT)으로 확정. **라운드 81 이후 서술 정정**: "cover 자유도가 거대하다"는 candidate 궤도 수를 옮겨 적은 것이었고 실제 해는 대부분 100개 이하다. 라운드 85(순서)와 87(용량)이 각각 따로 쓰던 short-edge 예산 2를 **공유**시켜 모든 cover 에 판정했으나 UNSAT 13개는 라운드 85 의 13개와 **sid 단위로 동일**(양방향 차집합 0) — 새 폐쇄 0. SAT 상태의 예산 사용 `{0: 2544, 1: 2846, 2: 1254}` 로 제약은 타이트하지만 2가 정확히 충분하다 | `research/RR_UNIVERSAL_COVER_CLAUDE.md` || 89 | **FORCED CORE 전수 census — 폐쇄 0, 그러나 동적 유일 cover 19개.** 궤도마다 금지/강제로 260회 풀던 것을 cover 완전 열거 1회로 대체(FORCED = 모든 cover 의 교집합, FORBIDDEN = candidate ∖ 합집합)해 **6,657 전수, UNKNOWN 0, 938초**로 확정했다. **2,637 상태(39.6 %)가 FORCED 궤도 보유**(최대 20) — 라운드 88 의 표본 39.7 % 가 전수에서 확인됐고, 5,716 상태는 FORBIDDEN 이 40개 이상, cover 해가 정확히 1개인 상태도 24개다. forced target 만 놓고 **가장 관대한 source 우주**(A ∪ 모든 candidate, OPTIONAL 포함, 각 5 port)에서 Hall 결손을 재니 **2,637 전부 0** — 그래서 §4 predecessor/entry cut 은 완전 매칭의 함의로 0이고, §5 optional 지원 조건은 optional 이 중간 source 가 될 수 없음을 증명하기 전에는 건전하게 쓸 수 없다. `FORCED ≥ 1` 이고 cover ≤ 20 인 tight 1,190 상태에 라운드 88 의 결합 필요조건을 모든 cover 에 적용한 결과 `{0: 13, 1: 19, 2-5: 115, 6-20: 1043}`, INCOMPLETE 0. **0개인 13 은 라운드 85 의 13 과 sid 단위 동일 → 새 폐쇄 0.** **새 표적은 동적 허용 cover 가 유일한 19개 상태** — 최종 궤도 집합이 결정되므로 라운드 81~88 을 죽인 '다른 cover 가 병목을 피해간다'가 구조적으로 불가능한 첫 사례다(예산 소진 `{2: 15, 1: 4}`, `c ∈ {2,3}`, forced ⊆ 유일 cover 19/19). 다만 19는 잔여의 0.29 % 이며 전부 닫혀도 6,625 다. 잔여 **6,644** | `research/RR_FORCED_CORE_CLAUDE.md`, `outputs/rr_forced_core_unique_states.json` || 90 | **단일 short pass 정리 — 예산이 틀렸다. 62개 폐쇄, 잔여 6,582.** 유일-cover 19개를 부검하다가 라운드 85 의 `ℓ<5 ≤ 2` 가 잔여에서 결코 달성될 수 없음을 발견했다. 리터럴 상태에서 **6,657 전부** 현재 육각형의 방문 칸이 정확히 1개이고, `F = 1 = TARGET_F` 라 abandonment 가 금지되므로 no-abandonment(`σ(w)` 방문 필요)가 현재 pass 의 `ℓ=5` 를 강제한다. 120 육각형·`P=121` 이므로 정확히 한 육각형만 2회 pass 를 받고 그것은 fragment 이니, **미래 `ℓ<5` edge 는 fragment 가 있으면 1개, 없으면 0개**이며 그 출발 port·회전 길이 `5−c_f`·목표 후보 4개까지 상태가 결정한다. 예산 census `{1: 5947, 0: 710}` — 상한 2 를 쓰는 잔여 상태는 **하나도 없다**. 계수 항등식 `121−P == 빈 육각형 + fragment` 가 6,657 전부에서 성립(정리의 독립 확인). 19개는 **모든** 정적 cover 를 port 수준 완전 열거로 판정해 ~~**19/19 UNSAT**~~ → **15/19** *(라운드 90c 에서 4개 폐쇄 철회)*. 정정된 예산으로 라운드-88 결합 조건을 전수 재판정하니 **71 UNSAT / 0 UNKNOWN**(24초), 이미 닫힌 13 을 빼면 ~~**새 폐쇄 62**~~ → **58** *(라운드 90c 정정)*. 결정적 통제: 같은 파이프라인을 예산 2로 돌리면 라운드 85/88 의 13 이 **sid 단위로 정확히 재현**되므로 차이는 오직 정리에서 나온다. 부수 정정: slack-cover 블록이 `U` 안에서 겹칠 수 있어 라운드 88/89 의 cover 수는 집합이 아니라 도출 수였다(19 는 하한, 19 중 18개는 정적 cover 집합이 애초에 1개). 잔여 ~~**6,582**~~ → **6,586** | `research/RR_SINGLE_SHORT_PASS_CLAUDE.md`, `outputs/rr_single_short_pass_claude.json` || 90c | **Codex 감사 반영 — 정리는 확인, 4개 폐쇄는 철회.** Codex 가 독립 재현한 것: 정정된 상태별 short 예산 `future_short_budget = 1 − r` **CONFIRMED**(아카이브 6,657 에서 예산 1 이 5,947 · 0 이 710, 예외 없음), 균일 예산 2 에서 **13**(라운드-85 SID 집합과 동일) **CONFIRMED**, 정정 예산에서 총 **71** **CONFIRMED**, 따라서 **새 폐쇄 58** — 내가 보고한 62 가 아니다. **`19/19 UNSAT` 은 REFUTED**: 독립 결과는 **15 UNSAT / 4 SAT** 이고, 네 상태 `ed0e44ff…`, `6afa15d9…`, `afb08178…`, `b3cd052f…` 는 최소 필요 예산 1 = 가용 예산 1 로 **등호에서 모든 검증된 조건을 동시에 만족**하는 witness(유일 cover · G5 생성 순서 · 단사 source-port 배정 · 합법 short 예외 1개)를 가진다. 문서 표현도 정정: 부분 육각형이 항상 2개인 것은 아니며, `r=0` 이면 fragment + 현재, `r=1` 이면 현재 하나뿐이다. **정리 자체는 무효가 아니다** — 라운드 90 의 58개 폐쇄는 예산 계수만 쓰며 그대로 유효하다. 네 상태는 **적대적 경계 witness** 로 보존한다. 감사된 잔여 **6,586** | `outputs/rr_boundary_witnesses_claude.json` || 91-A | **네 경계 witness 부검 — 새 폐쇄 0, 제약 하나 확보(감사 대기).** 라운드 90 과 Codex 가 갈린 지점을 정확히 짚었다: 라운드 90 의 M2 는 short edge 의 **개수**만이 아니라 **자리**까지 고정했다. 네 상태의 한-short witness 를 완전 열거하니(상태당 398,976~400,001, `b3cd052f` 는 캡 미도달로 완전) short edge 는 서로 다른 24~30개 port 에서 출발하는데 **fragment 육각형에서 출발하는 witness 는 0개**다. 리터럴 분류: 33.6만~35.6만은 그 시점에 **빈** 육각형에서 출발(거기서 `ℓ<5` 로 떠나면 abandonment 이고 `F=2 > TARGET_F`), 4.0만~6.7만은 **가득 찬** 육각형(미방문 시작 칸이 없어 발사 불가), `b3cd052f` 의 15,360개는 **현재 육각형**(엔진 확인상 그 pass 는 `ℓ=5` 만 합법). 회전 수준 사실은 전수 확인(720 단어 × 6 길이, 위반 0). 이로부터 **분류 B** 와 후보 제약 **SHORT-PASS LOCALITY** — `ℓ<5` edge 는 그 시점 부분 방문된 육각형에서 출발해야 하고 `F=1` 아래 그것은 fragment 뿐이며 시작 칸과 회전 길이가 고정된다 — 를 얻었다. **Codex 감사 대기 상태로 두고 원장에 쓰지 않았다.** 유계 헤드룸 진단(무작위 200, 노드 캡 15만): SAT 193 / UNSAT 2 / UNKNOWN 5 → 감사를 통과하면 약 1 % 추가. §10 정정: `D == 5·O − P` 가 6,657 전부에서 성립하고 `TARGET_D = 4` 이므로 '125 중 121 등록' 은 **새 좌표가 아니라 기존 `D`** 다(등록 port ≠ 방문 window). 잔여 **6,586** 유지 | `research/RR_BOUNDARY_WITNESSES_CLAUDE.md` || 92 | **fragment-local short 기회 — 186개 폐쇄, 잔여 6,396, 그리고 short 계열 소진.** Codex 가 SHORT-PASS LOCALITY 와 네 경계 witness 폐쇄를 확인해 기준선이 6,582 가 됐다. 라운드 88/90 이 short 예외를 `Gshort ∖ G5` 의 아무 edge 로 허용하던 것을 상태별 `SHORT_LOCAL(s)` 로 교체했다 — fragment 의 유일한 미방문 run 시작 칸에서 `ℓ = 5−c_f` 회전 후 4 joint. census: 예산 1 이 5,947 · 0 이 710, `c_f` 는 1~5(`c_f=5` 면 `ℓ=0`, 1,487개), **상태당 source port 정확히 1개 · 서로 다른 target 궤도 3개 · 그중 fresh 가 1~2개**(fresh 0 인 상태 없음). 과대근사 유지를 위해 `w2` joint 와 착지 위상 후보를 좁히지 않았고, Codex 경고대로 '진입 마스크는 항상 부분적' 으로 강화하지 않았다. **통제 먼저**: 같은 파이프라인이 일반 예산 2 에서 13개(라운드 85 와 SID 일치), 상태별 예산 + 일반 `Gshort` 에서 71개(라운드 90 과 SID 일치)를 재현한 뒤에만 새 모델을 썼다. 본 스윕 6,582 → **SAT 6,396 / UNSAT 186 / UNKNOWN 0, 33초**. 폐쇄는 `c=2` 105 · `c=3` 78 · `c=4` 3, root `short_ell2` 78 · `short_ell1` 61 · `short_ell3` 47, **전부 예산 1**. §7 원인 분해: **186/186 이 '일반 witness 의 short target 이 `SHORT_LOCAL` 밖'** — payoff 는 전적으로 target 제한에서 나오고 공유 port 규칙은 추가 폐쇄 0. §8 생존자 진단(완전 계수): 통과 cover 수 `{1: 64, 2-5: 1335, 6-20: 3782, >20: 1215}` 이고 **short 기회를 쓰는 통과 cover 를 가진 생존자는 0개** — 남은 상태는 short 없이 전부 `G5` 로 통과하므로 이 계열은 소진됐다. 다음 표적은 통과 cover 가 유일한 **64개** 상태. 잔여 **6,396** | `research/RR_SHORT_LOCAL_CLAUDE.md`, `outputs/rr_short_local_ledger.jsonl.gz` || 93 | **경로 순서 port 등록 — 육각형당 진입 칸은 하나. 1,366개 폐쇄(감사 대기), 잔여 6,396 유지.** 64개 유일 cover 를 집합 기준으로 재검증(64/64, 정정 없음)하고 `G5`-only / short-using 을 따로 완전 판정하니 **64개 전부 `G5`-only 실현 보유**(36 g5-only + 28 both) — 이 부분집합에서 short 구조는 무관하다. `extend()` 에서 직접 유도: 회전은 port 를 등록하지 않고 joint 만 **정확히 1개** 등록하므로 **pass 하나가 육각형 하나를 소비하고 진입 칸 1개만 등록**하며 나머지 5칸은 회전 내부로 소비돼 **영원히 등록 불가**다. 아카이브 전수 확인: 육각형별 등록 port 수가 빈 0 / 지나감 1 / **지나감 2 는 정확히 710**(= 이미 수리된 fragment 수) / 현재 1 / fragment 1, 예외 0. 따라서 남은 빈 육각형은 최종 25궤도 단어에서 정확히 한 번 진입돼야 하고 다음 진입 칸은 `joint(σ⁵(u))` 이며 각 pass 는 1회만 발사한다 — 남은 육각형 위의 Hamilton 경로 조건이고, 그 **완화인 이분 매칭의 Hall 위반**만 폐쇄에 썼다(캡·타임아웃 없음). 전형적으로 빈 육각형 107개 중 **105~106개가 최종 궤도 단어를 단 하나만** 포함해 진입 칸이 사실상 강제된다. 양성 대조 4종이 **실현된 이력**에서 6,657/6,657 통과(endpoint 가 등록 port · 다른 등록 port 에서 `σ^ℓ`+joint 로 도달 · 모든 등록 port 가 열린 궤도 안 · 육각형별 등록 수). 리터럴 해부: 상태 `14fda65d` 의 육각형 101 은 최종 궤도 단어가 1개뿐이고 그 전임자 4개는 2개가 비선택 궤도·2개가 이미 방문돼 **진입 자체가 불가능**하다. 모든 valid cover 에 결합+경로 조건을 함께 적용: 64 중 **13 UNSAT**, 전체 6,396 중 **1,366 UNSAT / 5,030 SAT / UNKNOWN 0**(235초), 결손 `{1:812, ...}` 은 ~~상태별 통계가 아니라~~ **마지막으로 평가된 cover** 의 값이었다(라운드 93c 정정; 상태별 최소 결손은 `{1:1206, 2:156, 3:4}`). **자기 정정**: 처음에 witness cover 하나로만 검사해 얻은 '72 % 거부' 는 상태 폐쇄가 아니라 cover 하나의 실패였다. ~~생존자의 통과 cover 수 `{1: 1760, ...}`~~ — **라운드 93c 에서 철회**: 첫 통과 cover 에서 멈추는 구현이라 완전 계수가 아니었다. 완전 열거값은 `{0:1366, 1:746, 2-5:2791, 6-20:1340, >20:153}`. **감사 대기이므로 원장은 6,396 유지** | `research/RR_PORT_PATH_CLAUDE.md`, `outputs/rr_port_path_ledger.jsonl.gz` || 93c | **Hall 감사 아티팩트 수리 — 새 정리 없음, 계수는 아카이브만으로 재현된다.** Codex 가 Hall 필요조건과 등록 의미론은 건전하다고 확인했으나 인증서 부족으로 1,366/5,030 을 재현하지 못했다. `outputs/rr_port_path_hall_archive/` 로 내보낸 것: 720 단어 기하 고정표(`ℓ=0..5` 의 joint target 포함), **6,396 상태**의 리터럴 자료(`p`·`hex_masks`·`orbit_masks`·`F/S/H`·`O/P/D`·`c/r/K/b`·fragment 점유와 수리 진입), 상태별 라운드-92 통과 cover **전부 90,396개**(도출 중복 제거, **첫 성공 절단 없음**), (상태,cover) 90,396쌍의 Hall 결과와 König 결손 부분집합, SAT **5,030**개의 **완전 매칭**, UNSAT **1,366**개의 **모든 cover 인증서**, `SCHEMA.md` 의 재구성 규칙. **표준 라이브러리만 쓰고 저장소 코드를 import 하지 않는 검증기**가 상태 행 검증(마스크·`P`·`O`·`D=5O−P`·현재 육각형 1칸·`121−P = 빈 육각형 + fragment`) 후 Hall 그래프를 처음부터 재구성해 **SAT 5,030 / UNSAT 1,366 재현, 저장값 불일치 0, 잘못된 SAT witness 0, 잘못된 UNSAT 인증서 0, 결손 히스토그램 일치**. **해석 정정 2건**: (a) '1,760개 상태가 Hall 통과 cover 1개' 는 첫 성공에서 멈춘 구현의 부산물이라 **철회** — 완전 열거값은 `{0:1366, 1:746, 2-5:2791, 6-20:1340, >20:153}`; (b) 결손 `{1:812,...}` 는 상태별 통계가 아니라 마지막 평가 cover 의 값이었고, 상태별 최소 결손은 `{1:1206, 2:156, 3:4}` 다. `14fda65d` 예시는 아카이브만으로 재생되어 육각형 101 의 유일 최종-궤도 단어의 전임자 4개가 2개는 비선택 궤도·2개는 **이미 방문**임을 보인다. **원장은 6,396 유지**(라운드 93 지위: HALL NECESSITY SOUND / STATE COUNTS CLAUDE-REPRODUCED / INDEPENDENT COUNT AUDIT PENDING) | `research/RR_PORT_PATH_HALL_AUDIT_CLAUDE.md`, `outputs/rr_port_path_hall_archive/SCHEMA.md` || 94 | **경로 연결성 — 층 A/B 는 값싸고 층 C 는 아니다. 잠정 228, 원장은 5,030 유지.** 라운드 93 Hall 은 배정만 보고 시간 순서를 보지 않는다. 의무(빈 육각형 + fragment) 위에 `x→y ⟺ x 의 진입 후보에서 σ^ℓ+joint 가 y 의 진입 후보에 닿음` 그래프를 세우고 현재 pass 를 뿌리로 두면, 유입 1·유출 ≤1 이므로 선택 간선은 (뿌리 경로)+(사이클)로 분해된다. **층 A**(뿌리 도달성) 쌍 실패 5,344, **층 B**(닫힌 부분집합)는 독립 구현에서 **모든 쌍이 A 와 동일** — 같은 조건이므로 따로 세지 않는다. **층 B2**(단일 경로의 끝점은 하나 ⟹ sink SCC ≥ 2 · 유출0 의무 ≥ 2 · 뿌리 유출 0 불가) 쌍 실패 1,796. 둘을 합쳐 **228 / 5,030 잠정 폐쇄, UNKNOWN 0, 24초**. **층 C**(Hall + 뿌리 연결 = 뿌리 Hamilton 경로)는 60상태 파일럿에서 **427/427 쌍 캡 도달 UNKNOWN**(146초) — 값싼 조건이 아니고 폐쇄 0이다(캡은 UNSAT 이 아니다). 양성 대조: 실현된 등록 port 중 다른 등록 port 에서 도달 불가한 것은 상태당 최대 1개(최초 단어)이고 2개 이상인 상태 0/6,396. **그러나 규명되지 않은 비대칭**: `r=1`(수리된 fragment) 거부율 **37.8 %** 대 `r=0` **0.7 %**. cover 수로 설명되지 않는다 — `r=1` 이 통과 cover 를 **더** 가진다(중앙값 7 대 3). §8 이 지목한 부류이므로 **규명 전까지 폐쇄를 원장에 올리지 않는다**. §11 분해: 유일-cover 746 중 20(2.7 %), 나머지 4,284 중 208(4.9 %) — 힘은 대안 cover 부재가 아니라 그래프 구조에서 나온다 | `research/RR_PATH_CONNECTIVITY_CLAUDE.md`, `outputs/rr_path_connectivity_ledger.jsonl.gz` || 94b | **r=1 비대칭 falsification — 버그 미발견, 원인 절반만 설명, 원장 변경 없음.** §9 귀속에서 방향이 바뀌었다: `r=1` 폐쇄 198 중 **190 이 순수 층 A**(뿌리 도달 불가)이고 B2 는 1개뿐 — 의심스러운 집중의 원인은 §4 가 지목한 sink-SCC 투영 보조정리가 **아니다**(`r=0` 30개는 대부분 B2). 표현 자체는 투영이 아니라 **동형**임을 확인했다 — 미래 pass 는 빈 육각형(+fragment)과 정확히 1:1 이고 매크로 edge 는 pass 시작에서 pass 시작으로만 가므로 생략되는 중간 꼭짓점이 없다. 남는 위험은 간선 완전성뿐이고 실현 이력 대조는 통과(도달 불가 등록 port 가 상태당 최대 1개, 2개 이상 **0/6,396**). 기전 실험: `r=0` 상태에서 **fragment 꼭짓점만 제거**하면 거부율이 0.7 % → **9.1 %**(4,476 중 407 폐쇄)로 오른다 — fragment 수리 edge 의 `ℓ=5−c_f` 오프셋이 만드는 연결성이 비대칭의 **약 1/4** 을 설명하지만 37.8 % 에는 못 미친다. §6 CONTROL R1-B(수리 육각형 두 등록 port 에 모든 `ℓ` 의 joint target 부여, 의도적 불건전 과대근사): **43 구제 / 155 잔존**. 모델에서는 그 두 port 가 이미 발사를 마쳤으므로(가득 참·현재 육각형 아님·port 당 1회) 미래 source 가 될 수 없다. §7 수동 해부와 §5 실현-이력 컷 재구성은 새 탐색이 필요해(§12 금지) 완료하지 못했다. **Outcome D — 228 전부 잠정, Codex 전달 보류, 감사 원장 5,030 유지** | `research/RR_R1_ASYMMETRY_DIAGNOSTIC_CLAUDE.md`, `outputs/rr_r1_asymmetry_claude.json` || 94c | **층 A 간선 완전성 — 누락 0, 그러나 여유 마진도 0.** 형식 진술 후 사건 분류를 전단사로 증명했다: 감사된 항등식 `121−P = 빈 육각형 + fragment` 로 **미래 pass ↔ 의무 정점이 1:1** 이므로 `G5`·`E¹`·`E²`·재진입·수리·`w2`/`w3` 어느 것도 '정점 없는 중간 구간' 이 될 수 없다(§2 의 위험 사례 C 소멸). §7 을 15쌍이 아니라 **전수**로 수행 — 228 후보의 모든 불도달 의무에 대해 전임자 **188,208** 건을 분류: 최종 cover 밖 155,481 · 합법이고 표현됨 19,077 · `ℓ` 기하 불일치 10,079 · 이미 방문 3,570 · source 육각형 비지 않음 1 · **LEGAL BUT OMITTED 0**. (첫 실행의 10,989 건은 분류기가 다른 cover 의 그래프를 참조한 **내 버그**였고 재바인딩 후 0이 됐다 — 판정 파이프라인이 아니라 분류기 문제.) `r=1` 수리 port 두 개가 이미 발사를 마쳤음을 엔진 불변량으로 증명(가득 참·현재 육각형 아님·pass 당 1회 발사·port 당 1회) — R1-B 는 불가능한 공급을 넣은 통제였다. **§9 `G_MAX`(모든 `ℓ` 허용)에서 227/228 이 구제된다** — payoff 전체가 감사된 `ℓ=5` 제약 하나에 달려 있고 여유 마진이 없다. §10 분리: 층 A 포함 **204** · B2 포함 32 · 둘 다 8 · 합집합 228, SID 집합 명시. §5 실현-이력 접미 검증은 **구조적으로 불가능**(아카이브에 접두만 있고 리터럴 접미가 없으며 새 탐색은 §12 금지) — 공백으로 보고한다. 게이트 판정 **결과 B**: 층 A 204개를 감사에 먼저 올리고 B2 32개는 보류, **원장 5,030 변경 없음** | `research/RR_LAYER_A_SOUNDNESS_CLAUDE.md`, `outputs/rr_layer_a_archive/` || 94d | **층 A 폐쇄 정정 — 204 철회, 196 확정, 잔여 4,834.** Codex 감사: 층 A 필요조건 자체는 **CONFIRMED** 이나 제출한 204 중 **8개는 층 A 로 닫히지 않는다**(A 를 통과하는 Hall cover 가 존재). **버그의 정확한 위치**: 상태 집계에서 `if any(v == "A" for v in verd)` 로 계산해 '**어떤** cover 가 A 실패' 를 폐쇄로 삼았다 — 올바른 술어는 '**모든** cover 가 A 실패' 이고, 그래서 다른 cover 가 B2 로 죽은 경우까지 섞여 집계가 **B2 로 오염**됐다. 로컬 재계산이 Codex 수치와 전부 일치: 상태 5,030 · Hall 통과 쌍 **27,095** · 층A PASS **21,751** · FAIL **5,344** · 상태 폐쇄 **196**(SID 일치). 지목된 예시 검증 — `5c461c58` cover 7 은 의무 107개 **전부 도달**(PASS), cover 10 은 92/107(FAIL). 8개 SID 를 옛/새 분류와 A-통과 witness 와 함께 보존. 정정 집합 산술(쌍이 아니라 상태 술어로 재유도): `A_closed` **196** · `B2_closed` **186** · 교집합 **154** · 합집합 228 — 이전의 '204/32/8' 은 폐기. **B2 는 승격하지 않는다**. 라운드 94c 의 건전성 결과는 보존된다(Codex 가 `121−P` 항등식 5,030/5,030, `ℓ` 범위, `r=1` port 재사용 불가, 188,208 분류, 확장 2,692,056 건에서도 누락 0, `G_MAX` 227/228 을 모두 재현). 생존자 census: 층 A 통과 cover 가 **정확히 1개인 상태 921** (`r=0` 4,500 / `r=1` 334). **감사된 잔여 4,834** | `research/RR_LAYER_A_CORRECTION_CLAUDE.md`, `outputs/rr_layer_a_archive_v2/` || 95 | **C1 후속 용량 · C2 차수 배정 — 둘 다 payoff 0, 잔여 4,802 유지.** A+B2 통과 cover 를 4,802 전부에 대해 완전 열거: `{1: 1069, 2-5: 2685, 6-20: 988, >20: 60}`, 총 쌍 **19,955** — Codex 의 `B2 PASS 19,955` 와 정확히 일치. **`UNIQUE_AB` = 1,069**(라운드 94 의 층-A-only 921 은 B2 적용 전 값이라 폐기). genuine path 의 선택 간선이 `({root} ∪ 의무∖{terminal}) → 의무` 의 **전단사**임을 유도해 두 층을 세웠다 — **C1**(terminal 하나를 뺀 모든 의무가 서로 다른 후속을 가짐), **C2**(Hall 포화 + **root 사용 강제**; Hall 은 root 미사용도 통과시키므로 진짜 강화). 결과는 **C1 pair_pass 19,955 / pair_fail 0**, **C2 pair_pass 19,955 / pair_fail 0**, **state_closed 0** — `r=0` 4,476 · `r=1` 326 양쪽 다 0이고 `UNIQUE_AB` 에서도 0. 공허한 이유: source `m+1` · target `m` 이고 root 가 모든 의무에 도달하므로 증가경로가 언제나 root 를 편입시킨다 — 차수 완화는 Hall+A+B2 를 넘지 못한다. **C3(subtour 제거)는 차수+subtour = 뿌리 Hamilton 경로와 사실상 동치**이므로 값싼 완화가 아니며 정확 경로 복잡도의 시작점으로 표시했다(라운드 94 파일럿에서 427/427 캡 도달). 잔여 **4,802** | `research/RR_DEGREE_ASSIGNMENT_CLAUDE.md`, `outputs/rr_degree_assignment_ledger.jsonl.gz` || 96 | **분리자 컷 — D1 이 4개(잠정), D2·E1 은 0. 값싼 무향 장애물 소진.** genuine 완성이 `{root} ∪ 의무` 위의 Hamilton 경로이므로 `U(G)` 는 spanning path 를 포함하고 **`c(U(G)−S) ≤ |S|+1`** 이 성립한다(root 를 정점으로 포함, 끝점은 root·terminal 둘뿐이라 off-by-one 없음). 전수 19,955 쌍: **E1**(root 아닌 무향 차수-1 정점 ≤ 1) `pair_fail` **0** — 차수-1 정점이 상태당 최대 1개(`{0: 19421, 1: 534}`)라 결코 발동하지 않는다. **D1**(`c(U−v) ≤ 2`) `pair_fail` **184** → **state_closed 4**. **D2**(`c(U−{u,v}) ≤ 3`, 저차수 쌍 제한의 건전한 부분검사) 추가 실패 **0**(UNIQUE_AB 1,069 파일럿도 0). §4 게이트대로 D3 은 중단; **D4 강제-간선 전파와 유향 source/sink SCC 상한은 미실시**(후자는 증명 전이라 쓰지 않음)로 명시한다. 양성 대조: 명시적 Hamilton 경로 + 임의 간선 **400개에서 오탐 0**. 진단: `UNIQUE_AB` 폐쇄 **0**(4개 전부 비-UNIQUE), `r` 분해 `r=0` 3 · `r=1` 1 로 **라운드 94 의 r 비대칭은 재현되지 않는다**. 감사 원장 **4,802 유지**, 잠정 잔여 4,798 | `research/RR_SEPARATOR_CLAUDE.md`, `outputs/rr_separator_ledger.jsonl.gz` || 97 | **강제-간선 전파 D4 — payoff 는 작고(4), 정확 Hamilton 이 처음으로 물었다.** rooted Hamilton 경로의 차수 사실(ROOT 유입0·유출1, 비-terminal 유입1·유출1, terminal 전역 1개)에서 규칙 5개를 세웠다 — R1 유일 유입 강제, R2 인증서 붙은 경쟁 간선 삭제, R3 유입 0, R4 유출-0 정점 2개/ROOT 유출 0, R5 **ROOT 없는 진부분** 강제 사이클. 임의 가지치기 없음. 전수 19,955 쌍: **D4a** `pair_fail` **446**(전부 R5) → `state_closed` **2**; **D4b**(= D4a 통과 후 강제 사슬 축약 → `c(U(G')−v) ≤ 2`; 축약은 강제 간선이 경로에서 연속이라 건전) `pair_fail` **714**(사이클 446 + 축약 분리자 268) → `state_closed` **4**. **라운드-96 D1 4개를 저장소 모듈로 처음부터 재계산해 4/4 재현**(독립 union-find 성분 계수, 저장 인증서 직접 검증, 쌍 실패 184·`r` 3/1 일치). **D1 ∩ D4b = ∅, 합집합 8** — 두 조건은 상보적이다(D4b 폐쇄 4개는 전부 `UNIQUE_AB`, D1 4개는 전부 비-`UNIQUE_AB`). 양성 대조: 명시적 rooted Hamilton 1,200개에서 세 층 모두 **오탐 0**, 파일럿 DFS 300개에서 **거짓 UNSAT 0**. payoff 가 작은 이유를 진단으로 고정: 의무 107–108개 그래프에서 유입 차수 최빈값이 **3**이고 유입 1인 의무가 쌍당 평균 **3.25개**, 아예 없는 쌍도 145 — 강제 전파는 원리적으로 약하다. §14 파일럿(UNIQUE_AB 한정, 캡=UNKNOWN): 200k 캡 60개 중 **UNSAT 1**, 3M 캡 12개 중 **UNSAT 4 / UNKNOWN 8 / SAT 0**, 라운드-94 층 C 의 427/427 UNKNOWN 과 대비된다. 파일럿 UNSAT 4개는 앞의 8개와도 **서로소**. 상태 판정은 전부 '모든 잔여 cover 실패' 로만 했다. **감사 원장 4,802 유지**, 잠정 잔여 4,790 | `research/RR_FORCED_EDGE_CLAUDE.md`, `outputs/rr_forced_edge_ledger.jsonl.gz` || 98 | **정확 rooted Hamilton — SAT 이 다음 불변량을 가리킨다.** 먼저 §0 의 취약 테스트를 고쳤다 (라운드-90 fragment 픽스처가 앞쪽 60개 표본에 우연히 들기를 기대하던 것을 `sid` 정렬 + 전수 결정적 선택 + 전제 미충족 skip 으로 교체; 수학적 의미 불변). **§1 재계수**: D1/D4b 는 쌍 판정이라 cover 를 개별 제거하므로 유일-cover 상태가 오히려 늘어난다 — 상태 **4,790**(감사값과 일치) · 잔여 쌍 **19,172**(19,955 에서 −783) · **`UNIQUE_PATH` 1,112**(1,069 폐기), 히스토그램 `1`:1112 `2-5`:2690 `6-20`:942 `>20`:46. **§3 난이도 예측자**: 간선 수와 ROOT BFS 깊이 **두 정수**로 12개 파일럿이 완전히 갈린다(UNSAT 274–282 간선·깊이 14–17, SAT/UNKNOWN 287–316·깊이 8–11) — 모집단 검증에서도 UNSAT 은 **전부 `간선 ≤282` 밴드**. **§6 엔진**: 비트셋 DFS + 정확 상태 키 memo + **역방향(suffix) 정식화**(ROOT 유입 0 이라 `G^R` 에서 자동 terminal); 양성 대조 250개 거짓 UNSAT 0, 두 정식화 150개 전부 일치. 방향 비대칭이 극단적이다 — 라운드-97 UNSAT 3개가 역방향 **113 노드**, 반대로 `a34d6499` 는 정방향 **1,418 노드 SAT**. **§4/§9 스윕**: 전체 1,112 캡 100k → SAT **67** · UNSAT **4** · UNKNOWN 1,041; easy 층(간선 ≤282) 130개 캡 1M → SAT **25** · UNSAT **7** · UNKNOWN 98. hard control `77fbaf89` 를 3M 양방향으로 완결. **새 잠정 폐쇄 8**(전부 `UNIQUE_PATH` 이므로 유일 cover 소진 = 상태 폐쇄; `r=0` 5 · `r=1` 3), 잠정 잔여 4,782. **§8 subtour-cut**(외부 solver 없이 배정+사이클 분기): UNSAT 에는 약하나 SAT 에는 압도적 — DFS 가 3M 에서 못 푼 `72f19d2f` 를 **10 iteration 0.01초**에 SAT. **§5/§14 핵심**: SAT 이 확정된 상태 **87**(UNSAT 12 의 7배), 검증된 증인 4개를 단어 수준으로 들어올리는 DP 는 **4/4 실패**(47·63·89·89번째 칸)이고 단계별 가능 단어 수는 **최대 1** — 즉 육각형 순서를 정하면 단어는 거의 강제되며, 놓친 필요조건은 **의무 그래프를 단어 위에 세우고 육각형당 단어 하나를 강제하는 것**이다. 감사 원장 **4,790 유지**, 테스트 **167 전부 통과** | `research/RR_EXACT_HAMILTON_CLAUDE.md`, `outputs/rr_exact_hamilton_ledger.jsonl.gz` || 99 | **단어 lift 는 결정적이다 — 그리고 그 결정성이 552개를 닫는다.** **정리(전수 4,320/4,320, 예외 0)**: 임의의 단어 `u` 와 `ℓ∈{0..5}` 에서 `σ^ℓ(u)` 의 네 joint target 은 **서로 다른 네 육각형**에 있고 어느 것도 source 육각형이 아니다. 따라서 `WORD_NEXT(u,ℓ,h)` 는 **부분 함수**이고, `ℓ` 이 이미 강제돼 있으므로(라운드 90/91) **고정된 현재 단어 + 고정된 의무 경로 ⟹ 구체 단어열은 많아야 하나**다 — 라운드 98 의 '단계마다 후보 ≤1' 은 artifact 가 아니라 정리다(§3 답 **A**). **§2 재현**: 네 실패가 깊이 46·62·88·88 로 결정적으로 재현(라운드 98 은 1-기반 47·63·89·89). **§14 분류**: 60개 실패 증인 **100 % 부류 A**(직전 구체 단어에서 그 의무로 가는 joint 자체가 없음) — B2/C/D–H 는 한 번도 발동하지 않았다. **§6 정정**: 보존된 87개 증인 중 **27개는 실제로 들어올려진다** (라운드 98 의 '4/4 실패' 는 표본이 작았다); 증인 하나의 실패는 폐쇄가 아니며 `72f19d2f` 는 lift 인지 탐색이 **다른 경로로 SAT**. **§15 payoff**: 결정성 덕에 그래프 층 A 를 단어로 옮긴 **W-A**(모든 의무가 현재 구체 단어에서 단어 전이로 도달 가능)를 전수 적용 — 쌍 19,172 중 **3,391 실패**, **상태 폐쇄 560**(라운드 98 감사분 8 제외 시 **새 잠정 552**; `r=0` 404 · `r=1` 148; 유일 cover 145 · 다중 cover 407 은 **모든** cover 실패), 930초. `r=1` 폐쇄율 46 % 대 `r=0` 9 % — 라운드 94 의 미규명 `r=1` 비대칭에 **구조적 설명**이 처음 나왔다. **§9/§11**: 상태를 `(현재 구체 단어, 남은 의무)` 로 둔 lift 인지 완전 탐색이 표본 14개 중 **11개 결정**(그래프 전용 5), 라운드 98 이 3M 노드로 얻은 `77fbaf89` 를 **1 노드**로. **대조**: 엔진이 만든 합법 사슬 121개 거부 0, lift 통과 증인 27/27 정적 통과, 표준 라이브러리 검증기가 기하 4,320 위반 0 · 원장 1,922 쌍 불일치 0 · SAT 사슬 단계 대조 통과 — 다만 **같은 저자의 두 번째 구현이므로 독립 감사가 아니다**. 감사 원장 **4,782 유지**, 잠정 잔여 4,230, 테스트 **172 전부 통과** | `research/RR_WORD_LIFT_CLAUDE.md`, `outputs/rr_word_lift_static_ledger.jsonl.gz` || 100 | **재생 감사 — 552 는 정확히 재현되고, `r=1` 설명은 철회한다.** **§0**: '1,922' 는 오타가 아니라 폐쇄 560행 **안의** 쌍 수였다 — 그러나 그것은 라운드-99 검증기가 PASS 쌍 15,781 을 한 번도 재판정하지 않았다는 뜻이므로 이번에 19,176 쌍 전부를 다시 판정했다. **§1 구조적 증명**: joint 네 개를 문자열로 쓰면 `T1=x2x3x4x5x1x0`, `T2=x3x4x5x1x2x0`, `T3=x3x4x5x2x0x1`, `T4=x3x4x5x2x1x0` 이고 육각형은 순환 회전류이므로 순환 후속 함수 여섯 번 비교로 **열거 없이** 정리가 증명된다(`x5` 의 상이 `x0` 이 아님 / `x1→x0` 대 `x1→x2` / `x5→x1` 대 `x5→x2` / `x2→x0` 대 `x2→x1`). **§2 두 번째 구현**: 문자열에서 직접 계산한 `WORD_NEXT` 가 아카이브 테이블과 4,320 문맥 **불일치 0**. **§3/§4 독립 재구성**: 층 A·B2·D1·D4b 를 새로 구현해 쌍을 다시 만든 결과 쌍 PASS **15,781 정확 일치** · 상태 SAT **4,230 정확 일치** · 폐쇄 564(라운드-97 감사분 4개를 건너뛰지 않아 +4). **§5**: 감사분 12 를 뺀 **552 SID 집합이 양방향 차집합 0**, 공통 1,922 행 판정 불일치 0. **§6 `r=1` 정정**: 라운드 99 의 '수리된 fragment 가 진입 단어를 고정한다' 는 **틀렸다**(`r=1` 에는 fragment 의무가 아예 없다). 쌍당 실패율 `r=0` 11.4 % 대 `r=1` 83.8 % 는 cover 수 artifact 도 `c` 교란도 아니며, 절제 실험이 기전을 지목한다 — `r=0` 의 fragment 는 `ℓ=5−c_f` 로 출발해 **다른 joint target 집합**을 갖는 **중계기**이고, 그 유출을 끊으면 실패율이 12.8 % → **53.1 %**, `ℓ=5` 로 정규화하면 **49.3 %** 로 뛴다. **알려진 의존성**: 모든 `ℓ` 을 허용하는 과대허용 대조에서 **564/564 폐쇄가 전부 소멸** — W-A 의 힘은 라운드 90/91 의 `ℓ` 강제 정리에서 나온다. **§7**: 문자열 구현으로 만든 합법 사슬 **1,379개**(길이 3–74, fragment 242 단계, `ℓ` 0–5 전부) 거부 **0**. **§10/§11**: `(현재 단어, 남은 의무)` 가 모델의 **정확한** 상태임을 보이고, memo 대 no-memo 33개 표본에서 결정된 판정 **불일치 0**. **§8/§12/§13**: 옛 hard control 8개 중 4 UNSAT 완결·1 SAT, 그래프-SAT 25개 중 **6개가 단어 수준 완전 소진 UNSAT** — 다만 전부 1 노드에서 끝나므로 **LIFT-DFS 가 W-A 위에 더한 폐쇄는 0**이다. 감사 **4,782 유지**, Claude 재생 잠정 **4,230**, 테스트 **180 전부 통과** | `research/RR_WORD_LIFT_REPLAY_CLAUDE.md`, `outputs/rr_word_lift_archive/` || 101 | **단어 도메인은 크기 ≤2 이고 96.9 % 가 singleton — 국소 일관성은 이미 소진됐다.** §1 W-A 생존 재구성: 통과 쌍 **15,781** · 상태 **4,230** · W-A 통과 cover 가 정확히 하나인 상태 **1,259**(라운드 98 의 1,112 는 W-A 이전 값이라 폐기). **§9 핵심 측정**: 의무 1,697,539개의 도메인 `|C(h)|` 이 **최댓값 2**, singleton **96.92 %**, 모든 의무가 singleton 인 쌍은 233(1.5 %). **§4/§5 W-C**(전임자 지지 P · 전역 terminal S · 재도달성 R · 빈 도메인 E, 고정점까지; 후속 부재로 개별 단어를 지우지 않아 ROOT/terminal 자유도를 정확히 존중): 쌍 실패 **5**(전부 S), **상태 폐쇄 0**, 쌍의 **91 %가 삭제 0**, 삭제 총량 1.7M 후보 중 ~1,688(0.1 %). **§6 W-D**(각 의무를 서로 다른 전임자가 공급하는 완전 매칭 — 라운드 93 Hall 의 단어 수준 정련): 쌍 실패 **0** — 라운드 95 C1/C2 가 공허했던 것과 같은 이유. **§10**: `r` 비대칭은 **W-A 가 이미 소비**했다(생존 `r=1` 269쌍의 평균 도메인 1.024 · 비-singleton 2.60 으로 `r=0` 과 구별되지 않음). **§12 양성 대조**: lift 통과 증인 **27/27 통과**, 엔진 사슬 585개 거부 **0**, AC 가 사슬 단어를 지운 경우 **0**. **§13**: 이 층들도 `ℓ` 강제 정리에 의존하며 모든 인증서에 `depends_on` 으로 명시했다. 원장 세 라벨 유지 — AUDITED **4,782** · CLAUDE REPLAYED **4,230** · 라운드-101 잠정 **0**. 테스트 **186 전부 통과** | `research/RR_WORD_CSP_CLAUDE.md`, `outputs/rr_word_lift_archive/wc_wd.jsonl.gz` || 102 | **이진 단어 배정 — `k ≤ 4` 이라 전수 열거가 되고, 그것만으로 178개가 닫힌다.** §1: 이진 차원의 완전 히스토그램이 `0`:233 · `1`:289 · `2`:1,698 · `3`:5,667 · `4`:**7,894**, **최댓값 4**(중앙값·p90·p99 모두 4), `Σ2^k` = **179,243** — 전 모집단 열거가 가능하다. 라운드-101 (P) 전파를 전처리로 돌리면 `k'=0` 쌍이 233 → **545** 로 늘고 `Σ2^k'` = 173,409. **§3 건전성**: genuine 완성은 실제 진입 단어로 배정 `A` 를 주고 그 경로는 `G_A` (간선 `h→g ⟺ WORD_NEXT(U_h,ℓ_h,g)=U_g`)의 rooted Hamilton 경로이므로, **모든 배정이 불가능하면 cover 불가능**이다. **§5/§6 W-E**(배정마다 이미 증명된 A/B2/D4/D1 을 건다): 173,409 배정 전수 344초, 쌍 FAIL **5,229/15,781 (33 %)**, **상태 폐쇄 178**(`r=0` 176 · `r=1` 2). **§17 지배 기전은 층 A(ROOT 도달성) 100,174** — B2 31,434 · D4 6,188 · D1 37 로, 배정을 고정하면 대부분 단순 도달성에서 죽는다. **§8–§13 정확 탐색**: 유일 W-A cover 1,259 상태를 `k'` 로 층화해 570쌍을 캡 20k 로 — SAT **22** · UNSAT **32** · UNKNOWN **516**. 그러나 UNSAT 32 는 **전부 정적으로 죽은 배정**이었고 Hamilton 완전 소진이 실제로 필요했던 cover 는 **0**, 즉 **정확 탐색이 W-E 위에 더한 폐쇄는 0**. `k'=0`(구체 그래프가 하나뿐인 대역)에서도 UNSAT 0 · SAT 10. **§18**: 육각형 그래프 대 구체 배정 그래프의 간선 평균이 303.3 대 **295.6(축소율 0.975)** — 도메인이 거의 singleton 이라 단어를 고정해도 그래프는 거의 안 줄고, 단어 정련의 힘은 그래프 축소가 아니라 **ROOT 도달성 판정**에 있다. 구체 Hamilton SAT 증인 22개를 보존했으나 **literal 엔진 재생은 이번 라운드에서 수행하지 않았다**(§15 는 열린 채로 남는다). 원장 — AUDITED **4,782** · CLAUDE REPLAYED **4,230** · 라운드-102 잠정 **178** → 잠정 잔여 **4,052**. 테스트 **192 전부 통과** | `research/RR_WORD_ASSIGN_CLAUDE.md`, `outputs/rr_word_assign_we.jsonl.gz` || 103 | **22개 SAT 증인은 전부 literal 로 재생된다 — 그리고 그것이 놓친 좌표를 드러낸다.** **§1/§2**: 라운드-102 의 구체 Hamilton 증인 22개를 `ExactState`/`extend` 로 직접 구동해 pass 단위로 재생 — 회전·joint·방문 창 무반복· port 재등록·`F/S/H`·`O/P/D`·fragment 타이밍을 엔진 자신이 검사하며 **22/22 LITERAL_REPLAY_PASS**, 실패 부류 A–F 는 한 번도 발동하지 않았다. 남은 회전을 마저 돌리면 **22/22 가 `visited=720 · F=1 · P=121 · O=25 · D=4`** 에 도달한다. **§3 그런데 `final_target` 은 22/22 실패**한다 — 마지막 조건 **`Ndef + H ≤ 3`** 이 측정값 **35–58** 로 깨진다(`Ndef = S+F−O`). **놓친 좌표는 방문 이력도 port 소비도 궤도 타이밍도 아니라 `N+H` 예산**이며, 원인은 네 joint target 의 비대칭이다 — 문자열 규칙에서 **`T1` 만 weight 2(무료)이고 `T2/T3/T4` 는 weight 3** (`dS=1`, `dH=0`). `ΔO = K` 가 고정이므로 미래의 weight-3 joint 수는 **`E ≤ B := 3 + K − Ndef − H`** 로 정확히 제한된다. 전수 측정: **`B` 중앙값 21**(18–24) 대 의무 107–108개 — 즉 **미래 pass 의 86개 이상이 유일한 무료 간선 `T1` 을 써야 한다**. 재생된 증인들은 `E ≈ 56–79` 로 예산을 크게 넘긴다. 가장 값싼 즉시 검사(`T1` 로 진입 불가한 의무 ≤ `B`)는 아직 한 번도 발동하지 않아(중앙값 4 대 21) 라운드 104 의 표적은 **가중 경로 제약**이다. **§5–§8**: 라운드-102 코드를 전혀 import 하지 않는 두 번째 구현(표준 라이브러리, 문자열 기하)이 `k` 히스토그램· 배정 **173,409**·층별 사망 `A` 100,174 / `B2` 31,434 / `D4` 6,188 / `D1` 37 · 쌍 10,552/5,229 · **폐쇄 178** 을 전부 재현하고 **SID 집합 차집합 0**. §8 검증도 통과(전부 W-A 생존 안, 552 와 교집합 0, cover 수 분포 `1`:106…`17`:1, 캡 기반 폐쇄 없음). 원장 — AUDITED **4,782** · CLAUDE REPLAYED **4,230** · 자가재생 **4,052**. 테스트 **199 전부 통과** | `research/RR_LITERAL_REPLAY_CLAUDE.md`, `outputs/rr_literal_replay.json` || 104 | **`N+H` 예산을 필요조건으로 — 전 잔여가 무너진다. 그래서 더 의심해야 한다.** **§0 예산 정리**를 사건 종류별로 재유도: `ΔF=0`(회전 뒤 σ-후속이 이미 방문돼 abandonment 없음 — 22개 재생 trace 에서 한 번도 안 나옴), `ΔS=E`, `ΔH=0`(최소 weight 사용 → 하한이라 안전), `ΔO=K` ⟹ `final(Ndef+H) = current + E − K` ⟹ **`E ≤ B := 3 + K − Ndef − H`** (중앙값 21, 18–24). **§3 검증**: 22개 증인에서 `E − B = final(Ndef+H) − 3` 이 **22/22 정확히 성립**하고 저장 경로 비용은 **54–75**. **§4/§10 값싼 하한**: 비용-0 매칭 결손 `L1` 중앙값 6 — **payoff 0**. **§12 비용-0 성분 하한 `L2 = c − 1`** 이 결정타 — 독립 계수로 확인하면 `T1` 호는 107개 중 **100개** (93.9 %)가 유효한데 성분은 **24개**(22–27), 즉 함수형 그래프에 **고리가 약 17개** 있어 경로가 고리마다 끊어야 한다. 뿌리에서 이미 `c−1 > B` 인 쌍이 **11,300/15,781**. **§15**: 라운드-103 에서 literal 로 완주한 22개 증인이 가중 재탐색에서 **22/22 UNSAT** — 저장 경로만 비싼 게 아니라 예산 안의 **어떤** 경로도 없다. **§6–§8 전수**: 배정 **173,409 전부 UNSAT**(SAT 0 · UNKNOWN 0, 캡 도달 0), 쌍 15,781 전부 FAIL, **상태 4,230 전부 폐쇄**, 121초. **결과가 전면적이라 오히려 경계한다** — 문서 §7 에 무효화 조건을 명시했다(최소-weight `ΔH=0` 가정, `ΔO=K`, pass↔의무 전단사, `ℓ` 강제, 그리고 **가장 큰 위험인 약한 양성 대조**: 목표 슬랩에 도달하는 합법 완성이 아카이브에 하나도 없어 진짜 양성 대조가 존재하지 않는다; 표본에서 나온 실제 경로 3개는 `B=실제비용` 에서 전부 SAT, 거짓 UNSAT 0). **감사 원장 4,782 는 손대지 않는다.** 라운드-104 잠정 잔여 0. 테스트 **207 전부 통과** | `research/RR_WEIGHTED_BUDGET_CLAUDE.md`, `outputs/rr_weighted_sweep.json` || 105 | **라운드-104 반증 시도 — 정리 서술은 틀렸고 수치는 살아남았다.** **§7/§8 성공한 공격**: 무작위 0/1 유향 그래프에서 모든 rooted Hamilton 경로를 전수 열거해 솔버와 대조한 결과 **42,052 (그래프,B) 쌍 중 111건이 `솔버 UNSAT / 실제 SAT`** — `L2 = c−1` 하한은 **일반 그래프에서 거짓**이다. 원인은 서술되지 않은 전제 **비용-0 유출 차수 ≤ 1**(성분 병합이 후속을 하나만 보므로 위반 시 성분이 과대계수된다). 제한을 건 절반에서는 불일치 **0**. **전제 검증**: 실제 인스턴스 **173,409 배정 전수에서 최대 비용-0 유출 차수 1, 위반 0** — 따라서 라운드-104 수치는 무효화되지 않으며, 코드에 `assert` 로 못 박았다. **§2 `ΔF=0` 도 정정**: 임의 시작점에서 `ℓ<5` 는 **항상** abandonment 를 일으킨다(ℓ=0..4 각 ~148k 문맥). 모델이 쓰는 `ℓ<5` 는 fragment 수리뿐이고, `r=0` 상태 **5,732개 전부**에서 조각 진입 단어 기준 `ℓ=5−c_f` 회전 뒤 σ-후속이 이미 방문된 칸임을 확인했다 (`c_f` 별 1,487/1,562/1,323/915/445, 예외 0). **§3**: 엔진 갱신표에서 `dH = max(w−3,0) ≥ 0` 이므로 **H 는 감소하지 못한다** → `ΔH=0` 은 안전한 낙관 완화(답 **A**). **§13 양성 대조를 3건에서 10,000건으로**: 명시적 Hamilton 경로를 심은 인스턴스 20,000 판정 **전부 SAT, 거짓 UNSAT 0**. **§12**: 22개 증인에서 `L1,L2 ≤ E` 위반 **0**. **§17**: union-find 판과 함수 사슬 추적 판 두 `L2` 구현이 6,692 배정에서 **완전 일치**. **§18 취약성**: 뿌리 마진 `L2−B` 분포 `−3`:162 `−2`:686 `−1`:1,414 `0`:3,313 **`1`:3,359** `2`:3,311 `3`:2,396 `4`:1,042 `5`:98 — **5,575/15,781 쌍은 뿌리 하한만으로 닫히지 않고**, 3,359 쌍은 마진이 정확히 1 이다. 즉 붕괴는 여유 있는 결과가 아니다. **미수행(정직한 공백)**: §16 전면 재계산, §19 의도적 구제 실험. **§20 의존성 DAG** 상 결론은 가장 약한 고리인 **Claude 자가재생** 지위를 물려받는다. 감사 원장 **4,782 불변**, 테스트 **215 전부 통과** | `research/RR_FALSIFICATION_105_CLAUDE.md`, `outputs/rr_falsification_105.json` |
| 106 | **폐쇄 경화 — 정정된 정리로 전면 재계산하고 자기완결 인증서를 낸다. 새 payoff 없음.** **§1/§2 정리 1(제한 성분 하한)**: 비용-0 유출 차수 ≤ 1 인 그래프에서 잔여 `R` 의 비용-0 약연결 성분이 `c` 개면 비용-1 호가 최소 `c−1`(진입 비용-0 호가 있을 때) 또는 `c`(없을 때) 든다 — 쓰인 비용-0 내부 호가 선형 숲이고 그 성분이 약연결 성분을 세분하므로 `|R|−y ≥ c`. 라운드 104/105 는 뿌리에서 항상 `c−1` 을 썼으나 두 번째 경우가 **더 강하고 여전히 건전**하다. **§4 정리 2**: 배정을 고정하면 의무마다 진입 단어가 하나이고 `ℓ` 이 강제되므로 `T1` 문자열이 유일 → 비용-0 유출 차수 ∈ {0,1}; 기하 정리(네 target 이 서로 다른 네 육각형)가 겹침도 막는다. 인증기는 믿지 않고 **정적 19,863,503 회 · 동적 73,435,904 회 확인, 위반 0**. **§5 정리 4 정정판**: `ΔF=0` 은 `ℓ<5` 의 일반 성질이 **아니고**(라운드 105 반증) `ℓ=5`(147,782/147,782)와 조각 수리(`r=0` 5,732/5,732)의 기하적 사실이다. **§7 `K = 25−O` 위반 0**, `B ∈ [18,25]` 대 의무 107–108. **§8/§9 전면 재계산 — 라운드-104 판정 JSON 을 한 줄도 읽지 않고**, 게다가 모집단을 **넓혀** A·B2·D1·D4b 를 쓰지 않은 **Hall 통과 상위집합 27,095 쌍 / 5,030 상태**에서 출발: W-A 생존 **17,350 쌍 / 4,232 상태**, 배정 **184,661 전부 UNSAT** (SAT 0 · UNKNOWN 0 · 캡 도달 0), 쌍 17,350 전부 FAIL, **살아남은 상태 0**. 아카이브 모집단으로 제한하면 **4,230 / 15,781 / 173,409** 로 라운드-100·104 수치를 정확히 재현한다. **§10** 뿌리 하한으로 안 닫히는 쌍 **5,771**(아카이브 모집단 5,122)은 전부 분기로 닫혔고 노드 합계 888,675 · 중앙값 57 · 최대 12,611(캡 2M 의 0.6 %), UNKNOWN 0. **§12 가장 값진 대조 — 정리 1 을 아예 쓰지 않는 솔버**(도달성 가지치기만)가 어려운 60개 중 **44개를 독립적으로 UNSAT 확정**(16개는 캡 → UNKNOWN, 불일치 0); 노드 1,398 대 4,149,351. **§13** 가설을 만족하는 작은 인스턴스 3,000개 전수 대조 **22,452 판정 불일치 0**; **§14** 경로를 심은 4,000 인스턴스 8,000 판정 전부 SAT, 접두 26,073개에서 동적 하한 위반 0; **§11** 실제 인스턴스의 잔여 5,898개 중 완성이 존재하는 823건에서 `lb ≤ C*` 위반 0(여유 0 이 529건 — 하한이 딱 붙는다). **§17 자기완결 인증서** 17,350 행 + 인증기를 import 하지 않는 **독립 검증기**(사슬 추적·인접 리스트·frozenset 메모)가 5,030 상태를 다시 걸어 **`VERIFIED — remaining states = 0`**; 다만 두 구현의 노드 수가 1,193,377 로 **동일**해 *코드*는 검증하되 *알고리즘*은 독립 검증하지 않는다는 한계를 명시했다. **§16 구제 진단 — 붕괴에 여유가 없다**: 예산을 1 만 늘리면(`B+1`) 배정 UNKNOWN 101,762, **상태 2,271 생존**(SAT 확인이 아니라 캡 도달). 허용된 최대 표현은 **'현재의 조건부 Q2/Area-A pass 모델 안에서 Claude 가 독립 재구현·재생한 인증서가 잔여를 0 으로 소진한다'** 뿐이다. 감사 원장 **4,782 불변**, 테스트 **239 전부 통과** | `research/RR_Q2_ZERO_CERTIFICATE_CLAUDE.md`, `outputs/rr_q2_zero_certificate.jsonl.gz`, `outputs/rr_q2_zero_verify.json` |
| 107 | **증명 추출 — 의존 사슬은 크게 줄었고, 서술되지 않은 가정 하나를 찾았다.** 새 폐쇄를 찾지 않았다. **(A) 줄어든 쪽**: 층 A·B2·D1·D4b 에 이어 **W-A · (P) 전파 · W-C/W-D/W-E · 정확 그래프-Hamilton · Hall 필터**가 전부 최소 증명에서 빠진다. 재계산으로 확인 — W-A 와 전파를 끄면 쌍 27,095 전부 · 배정 251,159 전부 UNSAT, Hall 까지 끄면 **쌍 90,396 전부 · 상태 6,396 전부 · 배정 707,007 전부 UNSAT** (SAT 0 · UNKNOWN 0 · 캡 도달 0 · 살아남은 상태 0). **fragment `ΔF=0` 기하 보조정리도 불필요**해졌다 — `F` 는 단조이고 아카이브 6,396 상태가 전부 `F=1`, 목표도 `F=1` 이므로 `ΔF=0` 은 목표에서 곧바로 나온다(보조정리 5). 남는 의존은 **상태·cover 열거 완전성**과 **pass↔의무 · `ℓ` 강제** 두 덩어리뿐이다. **(B) 찾아낸 가정**: 모델의 의무 그래프는 **weight ≤ 3 joint 네 개(`T1`–`T4`)로만** 호를 만드는데, 엔진의 indecomposable tail 은 weight 별 `1,1,3,13,71,461` = **550개**다. `cost(ω) = [ω≥3] + max(ω−3,0)` 이므로 weight-4 joint 는 비용 2 이고 **예산이 금지하지 않는다**(`B≈21`, 미래 joint 107–108). 실측: 배정 1,030개에서 무거운 joint 로만 닿는 (의무→의무) 호가 **12,062,293개**(비용 2: 461,531 · 3: 1,834,759 · 4: 9,766,003) — 배정당 비용-2 호만 평균 448개가 모델에 **빠져 있다**. **그래서 결론을 둘로 쪼갰다**: 비용-0 호는 전체 이동 집합에서도 `T1` 뿐이고(weight-2 tail 이 정확히 하나) 비용 ≥ 1 호는 각각 비용 ≥ 1 이므로, 성분 하한 `총비용 ≥ c(R)−1` 은 **무거운 호를 넣어도 그대로**다 → **정리 1: 모든 cover 가 뿌리 하한으로 닫힌 5,043/6,396 상태는 joint weight 와 무관하게 배제된다**; 나머지 **1,353 상태**의 폐쇄는 **가설 (H5) `모든 미래 joint 의 weight ≤ 3`** 에 조건부다(정리 2). **정정 두 건**: (i) 라운드-106 이 쓴 "쌍당 최대 노드는 캡의 0.6 %" 는 틀렸다 — 캡이 호출당이 아니라 **실행 전체 누적**에 걸려 있었고 실제 소진은 **59.7 %** 였다. 캡을 호출당으로 고쳐 전면 재계산해도 인증서는 **완전히 동일**(184,661 전부 UNSAT · 노드 1,193,377 · 캡 0). (ii) 그 버그 때문에 라운드-106 §16 이 "`B+1` 이면 2,271 상태가 되살아난다 / 붕괴에 여유가 없다" 고 잘못 적었다 — **철회**한다. 고친 뒤 `B+1` 과 `B+2` 에서도 **배정 184,661 전부 UNSAT · 캡 도달 0 · 살아남은 상태 0** — 예산 상수의 여유는 **최소 2** 이고, 뿌리 하한이 약해지는 만큼(11,579 → 7,660 → 3,870 쌍) 분기 탐색이 대신 일한다(노드 1.19M → 4.97M → 24.5M). 산출물: 최소 의존 DAG(간선마다 이유), 정의 D1–D18, 예산 정리의 완전한 기호 증명(정확한 항등식 대 낙관적 완화 구분), 임의 비음 비용에 대한 성분 하한과 손 검증 가능한 최소 반례, 탐색 완전성 귀납 증명, ∀ 집계 정리, **감사 표면 9항목**(1–3 은 이미 감사됨, 9 = (H5) 는 미해결), 논문 형식 정리 2개. **독립 감사자가 가장 먼저 공격할 단 하나의 주장 = (H5)**. 감사 원장 **4,782 불변**, 테스트 **263 전부 통과** | `research/RR_Q2_ZERO_PROOF_SKELETON_CLAUDE.md`, `outputs/rr_omitted_arcs.json`, `outputs/rr_q2_no_hall_certificate.jsonl.gz` |
| 108 | **(H5) 해소 라운드 — 가설은 증명되지 않았지만 대부분 필요 없어졌다.** **§1** (H5) 를 세 판본으로 분리: **A(local)** 무거운 joint 가 합법적으로도 못 일어난다 · **B(target)** 목표 완성이 그것을 포함하지 않는다 · **C(replacement)** 최소-weight 완성으로 치환된다. 인증서가 필요로 하는 것은 **B**. **§2/§3 단순 단조성 경로는 무효**: `H` 는 단조(`dH = max(w−3,0)`, 리터럴 19,727건 확인)이고 6,396 상태 전부 `H=0` 이지만, `final_target` 은 `Ndef+H ≤ 3` 만 요구해 **`H_최종 = 0` 이 아니다**(H 단독 3까지 허용). **§13 `H=0` 슬랩 따름정리는 존재하지 않는다** — `RR_PRUNE_TAXONOMY.md` 의 `H_positive` prune 은 **"Target A 인식기가 경계의 자식에 `H==0` 을 요구"** 한다는 근거로 Q1-safe 이며, 그것은 **경계까지**의 제약이다. 라운드-107 이 기대한 한 문단 corollary 는 없다. **§5 사건표**: 550개 tail(weight 별 `1,1,3,13,71,461`)에서 `cost(w) = ΔS + ΔH` 가 **19,727/19,727 정확**(하한이 아니다), `Δ(Ndef+H) = cost + ΔF − ΔO`. **§6 H5-local 반증**: 상태 `0018a7d3…` 에서 커서 `015432` 에 tail `w4:1230` 을 적용하면 목표 `321540` 이 **어떤 의무의 진입 단어**이고 `f1_prune_reason=None`, 직후 `Ndef+H=2` — 리터럴 반례를 보존했다. **§12 H5-replacement 반증**: 무거운 호로만 이어지는 (성분,성분) 쌍 **11,200** 대 가벼운 호가 잇는 **1,869**, 무거운 호 비율 97.1 % — 지배 정리는 거짓. **§4.1 새 보조정리(무거운-호 예산)**: `s := B − L2(ROOT)` 일 때 `Σ_{무거운 호}(cost−1) ≤ s`; **`s = 0` 이면 무거운 joint 를 하나도 쓸 수 없어 (H5) 가 그 인스턴스에서 정리**가 된다. **§9 정리 Z**: `cost(w)=0 ⟺ w=2` 이고 weight-2 indecomposable tail 이 **정확히 하나**이므로 전체 550개에서도 비용-0 호는 `T1` 뿐 — `G₀`·성분 하한·`outdeg ≤ 1` 이 그대로다. **§15 정리 R**: 그래서 **5,043 상태는 joint weight 와 무관하게 배제**된다(네-joint 이동 집합을 쓰지 않는 증명). **§10/§16 1,353 블록 완전-joint 재계산**(캡 2,500, 74분, (H5) 미사용): 배정 **240,756** → **UNSAT 234,851 · SAT 0 · UNKNOWN 5,905(캡)**; `s<0` 로 뿌리 폐쇄 110,930 + `s=0` 로 (H5)가 정리인 것 84,197 = **195,127(81 %)이 joint weight 무관**, 완전 그래프가 실제 필요했던 배정 45,629. 쌍 FAIL 31,410 · UNKNOWN 4,032 · **SAT 0**. **상태: UNSAT COMPLETE 1,187 / UNKNOWN 166 / SAT 0** → **분류 C (PARTIAL)**. **§11 census 는 비어 있다** (`heavy_edges_in_surviving_paths = {}`) — 무거운 호는 어떤 상태도 되살리지 못했고 탐색 공간만 부풀렸다. **§14 양성 대조**: 엔진 전이 5,870건 중 모델에서 빠진 것 **0**. **§17 감사 패키지** `outputs/rr_h5_audit/`(550-tail 생성기 · 비용표 · 1,353/5,043/166 sid 목록 · manifest). **원장**: AUDITED **4,782 불변**; **H5 무관 배제 5,043 + 1,187 = 6,230 / 6,396**, **H5 무관 잔여 ≤ 166**(라운드-107 의 1,353 에서 감소, 전부 캡 UNKNOWN). 남은 166개를 더 큰 캡으로 마저 판정하는 것이 다음 단계다. 테스트 **285 전부 통과** | `research/RR_H5_RESOLUTION_CLAUDE.md`, `outputs/rr_full_joint_certificate.json`, `outputs/rr_h5_audit/` |

---

## Standing methodological rules earned the hard way

1. **Supply-side upper bounds are fragile; demand-side lower bounds are not.** Any bound of
   the form "the walk cannot reach more than X" must model `q0` return and arbitrary repeats
   or it is unsound. (Round 73, after SKIP-COST.)
2. **Classify a candidate quantity under free `E¹` motion before proposing any inequality on
   it.** `E¹` freely repairs `D`, `P` and orbit-re-entry demand. (Round 77, after three
   rounds died to it.)
3. **A gate that checks one failure mode does not license a bound against a second.**
   SKIP-COST passed the hexagon-vs-port gate and still died to entry multiplicity. (Round 73.)
4. **When uncertain about a quotient graph, ADD edges rather than remove them.** Omitting a
   free move deletes legal transitions and inflates a lower bound. (Round 75.)
5. **Never treat a bounded continuation search as impossibility evidence**, and never treat a
   timeout as UNSAT.
6. **A round that reports state counts must preserve the per-state ledger and the instance
   archive — SAT and UNSAT alike — in a form an independent verifier can replay without
   importing our code.** Aggregate counters are not reproducible. (Round 80, after Round 79.)
7. **Never claim exhaustion**, and never derive a parity or congruence claim from corpus
   statistics. (Rounds 21–28.)
