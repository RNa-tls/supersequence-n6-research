# L6 엔드게임 라운드 144 — 독립 연구 보고 (Claude)

브랜치 `claude/n6-supersequence-length-rn17wf`.

> **증명 상태 경고.**  이 보고서의 어떤 문장도 `L6 = 872` 를 주장하지 않는다.
> 아래 폐쇄는 전부 **명시된 완화(상한 모형)** 안에서의 배제이고, 각 항목마다
> 무엇에 의존하는지 적었다.  캡/타임아웃에 걸린 탐색은 절대 UNSAT 으로
> 승격하지 않는다.  `NR6` 정규화 가정은 이 라운드의 어떤 결과에도 쓰이지
> 않았지만, 그것이 여전히 미증명이라는 사실도 변하지 않았다.
> **이 저장소는 아직 `L6 >= 872` 를 증명하지 않았다.**

---

## 0. 판정 요약

| 항목 | 판정 | 의존 |
|---|---|---|
| (FO) `L = 844+G+S+H` | **순수 계수로 재유도** | 없음 |
| MASTER-142 `L = 867+k+Z+H+B*` 가 (FO) 의 정확한 재배열 | **확인 (15,625 조합, 불일치 0)** | 없음 |
| 접합 정리 `K + R_int <= G+1`, `K = G+1 (mod 2)` | **독립 손 증명 + 6,000 무작위 사례** | 없음 |
| 조인트 목록 (자유 E / 120·201·210 / C·D / A·B) 완전성 | **문자열 겹침만으로 재유도, 720/720** | 없음 |
| 부분 pass 의 조인트도 같은 다섯 사상 | **전수 확인** | 없음 |
| 동반 육각 보조정리 | **720/720** | 없음 |
| 혼합 그림자 정리 `혼합 간선 <= D_j` | **720/720** | 없음 |
| 좌 `S6` 대칭 (시작 포트 고정) | **720 원소 전부 확인, 추이성 확인** | 없음 |
| 시그마 결손 정리 `D + 5b >= A` | **독립 증명 + 검증 (예리성 포함)** | 없음 |
| 끝점 표시 용량 `C(b,D,mask)` | **두 구현 일치, 전수** | 없음 |
| 결합 사슬 용량 `CC(b,D,a,bb,e)` | **전수 (캡 0)** | 없음 |
| `L = 867, 868, 869, 870` 배제 | **폐쇄** | 라운드 142 자원 회계 |
| `L = 871` | §7 | 〃 |

핵심 방법론 결론: **조각을 독립적으로 가격 매기면 869 조차 닫히지 않는다.**
남은 난점은 용량표를 키우는 것이 아니라 **조각들이 공유하는 자유도를 이중
계산하지 않는 것**이다.  이 라운드의 두 모형은 그 이중 계산을 두 단계로
제거한다: (i) 끝점 표시로 이음매를 결합한 **조각 모형**, (ii) A/B dirty 간선을
안에 남겨 이음매를 **문자 그대로** 만드는 **사슬 모형**.

---

## 1. 전제와 재유도

라운드 142 Route B 의 **정의**(선택 pass, `P=120+G`, `O=24+k`, `S`=유료
조인트 수, `H=sum (w-3)+`, `z=G-c=2g+d`, `Z=z-D2`, `B*=sum b_j+s`)는
저장소에서 복원했다.  그 위에서:

**(FO)** 를 순수 계수로 다시 얻는다.  선택된 창 720 개 중 `720-P` 개는 pass
내부 회전(문자 1 개), `P-1` 개는 조인트(문자 `w` 개)이고
`sum_joints w = 2(P-1) + S + H` 이므로

    L = 6 + (720-P) + 2(P-1) + S + H = 844 + G + S + H.

**MASTER-142** 는 `G = Z+D2+c` 와 `S = B*+O-1-D2-c` 를 (FO) 에 대입한 것일
뿐이다 (15,625 조합 검산, 불일치 0).  따라서 **오른쪽에 양의 항을 더해
강화할 수 없다** — 라운드 143 자신의 `threshold_closure:
NOT_ESTABLISHED_BY_ARITHMETIC` 경고와 같은 결론이다.

**접합 정리의 독립 증명** (`src/l6_incidence_144.py`).  원소는 `P` 개의
선택 pass 와 더미 하나, 총 `n = 121+G`.  `alpha = nu`(더미 고정)의 순환은
정확히 120 개 육각형(+더미)이므로 `c(alpha) = 121`,
`sum_h (m_h-1) = G`.  `T` 는 시간순 `n`-순환, `beta = T alpha^{-1}`.

* 부호: `sgn(alpha) = (-1)^G`, `sgn(T) = (-1)^{n-1}`, `sgn(beta) = (-1)^{n-K}`
  에서 `K = G+1 (mod 2)`.
* 경계: `T = beta alpha` 가 `n`-순환이므로 `<alpha,beta>` 는 추이적이고,
  육각형 121 개와 `beta` 성분 `K` 개를 잇는 이분 그래프는 **연결**이다.
  연결 이분 그래프는 변이 `121+K-1` 개 이상이고, 원소를 (육각형, 성분) 쌍으로
  세면 `121+G = E + R_int >= 121+K-1 + R_int`, 즉

      K + R_int <= G + 1.

  `R_int` 는 정의상 **성분 내부** 중복 육각 초과분이므로 다른 해석의 여지가
  없다.  추상 무작위 사례 6,000 개에서 위반 0.

---

## 2. 시그마 결손 정리 — 독립 증명 (GOAL A)

정리.  진입 포트가 모두 다른 스플라이스 경로에서 `A` 개의 시그마 간선,
`Q` 개의 열린 궤도, `P` 개의 포트, `D = 5Q-P`, 이미 열린 궤도로의 비-E 진입
`b` 개에 대하여 `D + 5b >= A`.

증명 세 단계 (`src/l6_sigma_deficit_144.py`):

1. **변환 항등식** `E^{-1} sigma = sigma^{-1} E` (720/720).
2. **누락 동반 포트의 전파**.  길이 5 의 E-run 이 `s` 에서 시작해 `E^4(s)` 로
   끝나면 다음 run 의 누락 포트는 `sigma^{-1}(s)`, 육각형 `h(s)`.  길이 4 run
   이 이어지는 동안 이 육각형이 보존되고, 다시 길이 5 run 이 오면 그 다섯째
   진입이 **E 간선으로** 이미 쓴 육각형에 들어간다 — 금지.  따라서
   `5,4,...,4,5` 는 불가능.  전수 확인 6,480 사례, 위반 0.
3. **군집 계수**.  시그마로만 이어진 극대 군집에서 `s >= f-1`, 따라서
   `sum (5-r_i) >= m-1`; 군집마다 합하면 `Delta >= A`.

**예리성**: `(5,4) -> (A,b,D) = (1,0,1)`, `(5,3,5) -> (2,0,2)`, 둘 다 여유 0.
따라서 `b=0` 에서 `D >= A` 를 균일하게 강화할 수 없다.

**한계**: 적대적 무차별 탐색은 6천만 노드에서 **CAP** — UNKNOWN_CAP 이며
전수성 주장이 아니다.  정리 자체는 위 손 증명으로 성립한다.

판정: **CLAUDE_SIGMA_DEFICIT 확인.**  다만 이 정리만으로는 870/871 의 어떤
행도 닫히지 않는다 (§6 의 폐쇄는 이 정리를 **쓰지 않는다**).

---

## 3. 조인트 목록과 부분 pass — 모형의 기반

`end(v) = sigma^{-1}(v)` 에서 나가는 최단 조인트를 720x720 쌍의 문자열 겹침과
숨은 순열 창 탐지만으로 다시 만들었다.  모든 `v` 에 대해 정확히

| 무게 | 표적 | 이름 |
|---|---|---|
| 2 | `E(v)` | 자유 (같은 궤도, 위상 +1) |
| 2 | `sigma(v)` | **A** dirty (같은 육각형) |
| 3 | `E^2(v)` | `120`, 유료, 같은 궤도 위상 +2 |
| 3 | 두 개 | `201`, `210`, 유료, 교차 궤도 |
| 3 | `E(sigma v)` | **C** 혼합 |
| 3 | `sigma(E v)` | **D** 혼합 |
| 3 | `sigma^2(v)` | **B** dirty (같은 육각형) |

이고 다른 것은 없다 (720/720).  대수적으로 만든 사상과 정확히 일치.

**부분 pass 가 모형을 깨지 않는 이유.**  포트 `v`, pass 길이 `l` 의 조인트
출발점은 `sigma^{l-1}(v) = sigma^{-1}(u)`, `u = sigma^l(v)` 이므로 **모든
조인트는 같은 일곱 사상을 `u` 에 적용한 것**이다.  전수 확인: `l < 6` 인 pass
의 깨끗한 `w in {2,3}` 후속은 **하나도 같은 궤도에 있지 않고** (각 `l` 마다
720+2160 개 전부 교차 궤도), `l = 6` 에서만 `E` 와 `120` 이 같은 궤도다.
이것이 라운드 142 successor splicing(`nu(i) -> i+1` 재배정)이 full-pass
모형을 정당화하는 기하학적 이유이며, 나는 그것을 가정이 아니라 계산으로
확인했다.

---

## 4. 세 개의 손 보조정리 — 전부 독립 확인

* **동반 육각**: `orb(v)` 와 `orb(sigma v)` 는 정확히 `{h(v), h(Ev)}` 를
  공유한다 (720/720).  A 이음매의 양쪽 clean-E 블록이 **둘 다 꽉 찬 5 포트**면
  두 조각이 공통 육각형 둘을 모두 포함하므로 경계 육각형 **외에** 반복이 하나
  더 필요하다.
* **혼합 그림자**: C 간선의 그림자 `sigma(v)` 는 표적 궤도에 있고 육각형이
  `h(v)`; D 간선의 그림자 `E(v)` 는 출발 궤도에 있고 육각형이 표적과 같다.
  둘 다 단사이고 교차 충돌 `sigma(v)=E(u)` 는 `sigma^2(v)` 가 `h(v)` 에 놓여
  육각 단순성을 깨므로 불가능 (720/720).  ⇒ **조각당 혼합 간선 <= D_j**.
* **좌 `S6` 대칭**: 값 치환 작용은 위치 회전과 가환이므로 `E, E^2, sigma,
  sigma^2, 201, 210, C, D` 모두와 가환이고 720 포트 위에서 추이적이다
  (720 원소 전부 확인).  따라서 **시작 포트 고정은 증명된 축약**이지 가정이
  아니다.

---

## 5. 두 개의 용량 모형

### 5.1 조각(끝점 표시) 모형

`C(b,D,mask)` = 육각 단순 full-pass 사슬의 최대 포트 수.  `mask` 는 첫/마지막
clean-E 블록이 **부분(5 포트 미만)이어야** 하는지를 기록한다.

* `src/l6_marked_capacity_144.py` — 파이썬, `tau`/`sigma` 로 대수적 사상, dict
  위상 마스크, 히스토그램에서 결손 재계산.
* `src/l6_marked_capacity_144.c` — C, 기하를 **문자열 겹침 스캔으로만** 유도,
  평면 배열·증분 결손.  `b=0,dmax=10` 에서 노드 **13,780,945** 로 파이썬과
  값·노드 수까지 일치.
* `src/l6_marked_capacity_pruned_144.c` — 두 개의 건전한 가지치기를 추가.
  **(S) 접미사 경계**: 노드의 현재 결손 `dc`, 남은 토큰 `t` 에서 나머지를 그
  자체로 사슬로 보면 `D_S = D_fin - dc + 4 + 5r`, `r <= t` 이므로
  `D_S <= DMAX - dc + 4 + 5t`, 따라서 총 포트 `<= P + U(t, DMAX-dc+4+5t) - 1`.
  **(B) 최고기록**.  `b=0,dmax=12` 에서 무가지치기 115,757,562 노드 대신
  883,170 노드로 **같은 값**.

주요 값 (전수, capped=false):

| D | 0 | 1 | 2 | 4 | 6 | 8 | 10 | 12 | 14 | 17 | 20 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `C(0,D,--)` | 20 | 20 | 33 | 46 | 49 | 62 | 70 | 83 | 96 | 103 | **106** |
| `C(0,D,부분·-)` | **불가** | 19 | 19 | 32 | 45 | 57 | 65 | 73 | 82 | 95 | 105 |
| `C(0,D,부분·부분)` | **불가** | 4 | 18 | 31 | 44 | 47 | 60 | 68 | 81 | 94 | 101 |

무표시 열은 옛 `N*(b,0,D)` 표를 칸마다 재현한다.  **`C(0,0,부분)=불가능`** 이
결합 논증의 심장이다: `b=0,D=0` 에서는 모든 궤도가 완전히 덮이므로 clean-E
블록이 부분일 수 없다.

행 평가는 조각 경로 위의 DP 로 하고, 제약된 이음매의 위치는 (완화이므로)
적대적으로 고른다.  `nmark = max(0, max(0,D2-d) - max(0,Z-Qs))`.

### 5.2 결합 사슬 모형 — 이중 계산의 제거

조각을 따로 가격 매기는 대신 **베타 성분 전체**를 한 번에 탐색한다.  유지된
A/B dirty 간선을 사슬 안에 남기고, 이미 쓴 육각형에 착지할 수 있는 것은

* A/B 간선의 표적(그 육각형이 **곧** 출발점의 육각형), 또는
* 반복 여유에서 값을 치른 **추가 충돌**

뿐이다.  그러면 동반 육각 보조정리를 손으로 부과할 필요가 없다: A 이음매 앞
블록이 꽉 차 있으면 `E(v)` 가 이미 점유되어 다음 궤도의 포트
`sigma^{-1}(E v)` 가 같은 육각형이므로 탐색기가 **자동으로** 막는다.
(`amax<=4`, `D<=5` 에서 용량이 `20,20,33,33,46,46` 로 **전혀 늘지 않는** 것이
그 직접 귀결이다.)

행 하나의 예산:

    사슬 수          = d + 1 + h        (더미 성분 + d 개의 순환, 그리고 h 개 heavy cut)
    sum 결손         = 5k - G + 5s
    sum 토큰         = B* - s
    sum A <= D2,  sum B <= Qs
    sum A + sum B + sum 추가충돌 <= R_int <= 2g
    추가충돌 <= max(0, Z - Qs)
    필요 포트        = 120 + G - 5c

`s` 의 해석은 조각 모형의 "조각 간 궤도 공유"에서 "사슬 간 궤도 공유"로
바뀌지만 `T + s = B*`, `sum D = 5k-G+5s` 는 그대로다 (보고서 §6 유도).
`추가충돌 <= Z-Qs` 는 `2g = D2+Z-d` 와 순환 개방 `x+y <= d` 에서 나온다.

> **자기 정정.**  이 모형의 첫 판은 A/B 표적에서만 육각 재방문을 허용했다.
> 그것은 완화가 아니라 **과도한 제약**이었고 `Z > Qs` 행에서 건전하지 않았다
> (조각 분해는 보통의 육각 충돌에서도 자른다).  추가 충돌 예산을 도입해
> 고쳤고, 고친 뒤에도 867..870 의 결론은 그대로였다.

---

## 6. 결과 (중간 — 최종 수치는 §9)

두 모형 모두 상한이므로 행마다 **둘 중 작은 값**을 쓴다.  계산되지 않은 칸은
육각형 수 상한 **120** 으로 대체하므로 판정은 언제나 건전하며, 대체값을 쓴
행은 따로 센다.  행 열거는 **시그마 결손 정리를 쓰지 않는다**(필터를 끄면 행
수가 늘 뿐이고 결론은 같다).

| 길이 | 필요 행 | 조각 독립 상한 | 조각 결합 상한 | **사슬 결합 상한** |
|---|---:|---:|---:|---:|
| 867 | 1 | 0 생존 | 0 생존 | **0 생존** |
| 868 | 16 | 0 생존 | 0 생존 | **0 생존** |
| 869 | 115 | **5 생존** | 0 생존 | **0 생존** |
| 870 | 552 | 106 생존 | **52 생존** | **0 생존** |
| 871 | 2046 | — | — | §8 |

미증명 칸 0, 캡 0.  따라서 **라운드 142 의 자원 회계를 전제로 `L6 >= 871`**
을 독립적으로 얻는다.  (라운드 143 의 표제 결과 `L6 >= 870` 도 그 안에
포함되어 독립 재확인된다.)

---

## 7. 모형을 결합하는 올바른 방법 — 자기 정정

추출 매개변수

    s = (객체별 궤도 수 합) - (순수 E 순환 밖의 궤도 수)

는 **덮개만의 성질이 아니다.**  어느 분해를 쓰는지에 달렸고, 조각·분할사슬·
병합사슬 분해에 대해

    s_m <= s_c <= s_p,   결손 예산 = 5k-G+5 s_M,   토큰 예산 = B* - s_M

이다.  그러므로 **서로 다른 모형을 같은 `s` 에서 평가해 최솟값을 취하면 건전하지
않다**: 어떤 모형이 자기 것이 아닌 `s` 에서 닫힐 수 있고, 그러면 실제 덮개를
대표하는 행이 아무 데서도 살아남지 않는다.  올바른 결합은 모형 독립 좌표
`C = (k,Z,H,B*,G,g,c,d,D2,Qs,h)` 로 행을 묶고

    bound_M(C) = max over (M 에 허용되는 s) of bound_M(C, s),
    배제  <=>  min over M of bound_M(C) < 120 + G - 5c.

여기에 한 가지 예리한 제약이 더 있다: **객체가 하나면 `s = 0` 이 정확히 성립**
한다 (그 객체의 궤도가 곧 순수 순환 밖 궤도 전체다).  분할사슬 모형의 객체는
`d+1+h` 개, 병합사슬 모형은 `d+1` 개로 행이 정하므로 이 제약이 그대로 쓰인다.
`src/l6_rows_final_144.py` 가 이 결합을 구현한다.

세 번째 모형 — **병합사슬** — 은 무게 `>= 4` 조인트를 자르지 않고 성분 안에
남긴다.  자르면 조각 모형이 `h` 개의 **공짜 객체**를 얻는데, 각 객체가 육각형
120 개 예산을 새로 받는 것이 상한을 크게 느슨하게 만든다.  안에 남기면 표적이
`end(v)` 에서 나가는 무게 4/5/6 최단 연결자 24/120/566 개(합 710)로 제한되고
예산 `sum (w-3) <= H` 를 쓴다.  값의 예: `CC_h1(0,15) = 106` 인데 같은 예산을
두 사슬로 쪼개면 `58+62 = 120` 이다.

> **자기 정정 2.**  병합 모형의 첫 구현은 heavy 예산을 재사용 예산과 같은 UB
> 색인에 넣어 접미사 경계를 건전하지 않게 썼고, 그래서 뿌리에서 곧바로 잘려
> 26 행이 "닫혔다"고 잘못 보고했다.  UB 에 heavy 차원을 분리해 고쳤다.

---

## 8. 871 에 남는 것 — 등호 증인과 순수 순환 공존 시험

바로잡은 결합에서 `L = 869` (좌표행 99) 와 `L = 870` (좌표행 437) 은 **전부
STRICT** 다 (미증명 칸 0, 대체값 0).  `L = 871` 에서 남는 좌표행은 상한이
필요 포트 수와 **정확히 같은** 등호 행들이고, 두 개의 용량 질문으로 모인다.

| | 구조 | 필요 | 상한 | 증인 수 |
|---|---|---:|---:|---:|
| **Q1** | 육각 단순 사슬 96 포트·22 궤도·결손 14, 순수 순환 `c=6` | 96 | 96 | **2** |
| **Q2** | 위 + heavy 조인트 하나, 92 포트·20 궤도·결손 8, `c=7` | 92 | 92 | **1** |

증인은 사슬 탐색기의 **증인 모드**로 전수 열거했다 (포트 수 = TARGET,
결손 = DMAX 를 정확히 만족하는 사슬을 전부 덤프; 그 모드에서는 기록
가지치기를 끈다 — 기록과 **동점**인 사슬을 버릴 수 있기 때문이다).  노드
16,555 / 5,775, 둘 다 capped=false.  좌 `S6` 가 포트 위에서 추이적이므로
시작 포트를 0 으로 고정한 것은 증명된 축약이고, 따라서 이 셋이 `S6` 를 법으로
모든 등호 사슬이다.  증인은 `outputs/witness_144/` 에 문자 그대로 보존한다.

### 순수 순환 공존 시험

이 행들에서 **접합 경계가 정확히 등호**다: `K = c+1`, `R_int = 0`, `G = c`,
따라서 `K + R_int = G+1`.  §1 의 증명에서 여유는 이분 그래프의 독립 순환 수였
으므로 등호는 그 그래프가 **나무**라는 뜻이다.  그러면

* 120 개 육각형이 **모두** 쓰인다 (안 쓰이면 반복 초과가 `G` 를 넘는다);
* `R_int = 0` 이므로 사슬은 육각 단순, 육각형 `P` 개를 쓴다;
* 남은 `|F| = 120 - P = 4c` 개 육각형은 `c` 개 순수 순환(전체 tau-궤도, 각각
  육각형 다섯 개)이 **덮어야** 한다.  순환의 궤도는 사슬의 궤도와 겹치지
  않는다("그 궤도는 다른 어디에도 나타나지 않는다").

세 증인 모두 이것이 **불가능**하다.  서로 다른 두 solver 로 확인했다:

| 증인 | 포트 | \|F\| | c | exact-cover DFS | 부분집합 열거 | 판정 |
|---|---:|---:|---:|---:|---:|---|
| Q1 #0 | 96 | 24 | 6 | 36 노드 | 1 노드 | 불가능 |
| Q1 #1 | 96 | 24 | 6 | 76 노드 | 1,028 노드 | 불가능 |
| Q2 #0 | 92 | 28 | 7 | 47 노드 | 519,948 노드 | 불가능 |

증인 Q1#0 은 손으로도 즉시 불가능하다: `|hex(orbit) cap F|` 의 상위 여섯 값이
`5,5,3,3,3,3` 이라 합이 22 인데 24 를 덮어야 한다.  (세 번째 검사로 넣은 평균
논증은 나머지 두 증인에서는 통하지 않는다 — 그 둘은 DFS/부분집합 열거가
결정한다.  그 사실도 그대로 기록한다.)

---

---

## 9. 최종 결과

### 9.1 행 평가 (모형 세 개, 올바른 결합)

| 길이 | 좌표행 | STRICT | 생존 | 대체값 쓴 생존 |
|---|---:|---:|---:|---:|
| 867 | 1 | 1 | **0** | 0 |
| 868 | 14 | 14 | **0** | 0 |
| 869 | 85 | 85 | **0** | 0 |
| 870 | 353 | 353 | **0** | 0 |
| 871 | 1,156 | 1,154 | **2** | 0 |

미증명 용량 칸 0.  대체값(육각형 상한) 사용 0.  시그마 결손 정리 **미사용**.
행 열거에는 `H >= 1 => h >= 1` (무게 `>= 4` 조인트가 적어도 하나 있어야 한다)
을 넣었다.

남은 두 행은 §8 의 Q1(`k=4, G=6, c=6, h=0`, 필요 96)과
Q2(`k=3, G=7, c=7, h=1`, 필요 92)이고, 세 모형의 상한이 모두 필요 포트 수와
**정확히 같다**.  두 행 모두 등호 증인이 전수 열거되고 순수 순환 공존 시험으로
배제된다.

### 9.2 증인 열거의 교차 확인 — 네 번, 전부 같은 파일

| 방법 | Q1 | Q2 |
|---|---|---|
| C 탐색기 + 부트스트랩 UB 표 | 2 개 / 16,555 노드 | 1 개 / 5,775 노드 |
| C 탐색기 **UB 표 없이** (결손·육각·목표 가지치기만) | 2 개 / 900,166,560 노드 | 1 개 / 149,792,913 노드 |
| 파이썬 **run 단위** 열거기 (기하를 tau·sigma 로, 경계를 자체 파이썬 용량표로) | 2 개 / 1,618,863 노드 | — |
| 파이썬, **강제 분할** 경로 (Q2 는 `(D1,D2)=(4,4)`, `(P1,P2)=(46,46)` 만 가능) | — | 1 개 / 536+790 노드 |

전부 capped=false 이고 **증인 집합의 SHA-256 이 완전히 일치**한다
(`wit_Q1`/`wit_Q1_noub`, `wit_Q2`/`wit_Q2_noub` 동일 digest).
Q2 의 강제 분할은 부수적으로 재미있는 사실을 준다: 포트 46 개·결손 4 인
육각 단순 사슬은 좌 `S6` 를 법으로 **딱 하나**다.

### 9.3 따라서

모든 `L <= 871` 후보가 배제된다.  전제는 라운드 142 Route B 의 **1차 발생
골격**이고 — 이 라운드에서 (FO) 항등식, MASTER-142 대입, 추출 회계, 접합
정리, 조인트 목록, 세 기하 보조정리, 좌 `S6` 축약을 전부 독립적으로 다시
유도·검산했다 — `NR6` 는 **어디에도 쓰이지 않는다.**

> 그러므로 이 라운드가 실제로 확립한 것은:
> **라운드 142 의 1차 발생 골격을 전제로, 870 과 871 이 닫힌다.**
> 인증서: `outputs/rr_l6_certificate_144.json`
> (용량 칸 55 + 1,501 개, capped 0, 노드 합 약 9.8 x 10^10,
> 원본·바이너리 해시와 표 digest 포함).

**나는 `L6 >= 872` 를 증명했다고 주장하지 않는다.**  §10 이 이유다.

---

## 10. 정직한 한계 목록

1. **이 저장소는 아직 `L6 >= 872` 를 증명하지 않았다.**  위 결론은
   "라운드 142 Route B 골격을 전제로 870·871 폐쇄"이며, 나는 그 골격의 손
   논증 **전부**를 이 라운드에서 다시 감사하지 않았다.  특히
   * B1 의 **고정 대표원** 축약(1차 발생 최대겹침 재표기를 반복하면 길이가
     줄거나 같고 고정점에서 모든 간격이 최소가 된다) — 손 논증을 §3 에서
     확인했지만 원문 수준의 전면 감사는 아니다;
   * B4 의 **successor splicing** 정리 — 기하학적 핵심(모든 조인트가 `u`
     에 적용된 같은 일곱 사상)은 전수 확인했지만, 그 재배정이 `beta` 구조를
     정확히 보존한다는 조합 논증은 라운드 139 감사 결과를 재사용했다.
2. 용량 모형은 **완화**다.  외부 그림자/이력 제약을 풀었으므로 상한을
   달성하는 사슬이 실제 덮개로 들어올린다는 주장은 하지 않는다.  "생존" 은
   실현 가능성의 증거가 **아니다**.
3. 시그마 결손 정리의 적대적 무차별 탐색은 6천만 노드에서 **CAP** 되었다
   (UNKNOWN_CAP).  정리 자체는 손 증명으로 성립하고, §9 의 폐쇄는 그 정리를
   **쓰지 않는다.**
4. Phase 5 가 요구한 **균일한 master 부등식**(예: `k+Z+H+B* >= 5`)은 얻지
   못했다.  사슬당 궤도 수의 손 경계 `O <= f(D)` 가 있으면 따라 나오지만
   나는 그 손 경계를 갖고 있지 않다 — 지금 있는 것은 전수 표뿐이다.
5. `NR6` 는 이 라운드의 어떤 결과에도 쓰이지 않았고, 여전히 미증명이다.
6. 이 라운드에서 **내 스스로 두 개의 건전성 오류를 찾아 고쳤다**: (i) heavy
   예산을 재사용 예산과 같은 UB 색인에 넣은 접미사 경계(§7), (ii) 모형마다
   다른 `s` 를 공통으로 보고 최솟값을 취한 결합(§7).  둘 다 고치기 전에는
   더 강한 결론을 주었다.  이 사실 자체가 남은 위험의 크기를 말해 준다 —
   독립 재감사가 여전히 유익하다.

---

## 11. 산출물

| 파일 | 내용 |
|---|---|
| `src/l6_master_identity_144.py` | (FO) 와 MASTER-142 |
| `src/l6_bookkeeping_144.py` | 추출 회계 재유도 + 20 만 사례 |
| `src/l6_incidence_144.py` | 접합 정리 손 증명 + 무작위 사례 |
| `src/l6_sigma_deficit_144.py` | 시그마 결손 정리 독립 증명·검증 |
| `src/l6_marked_capacity_144.py` / `.c` | 끝점 표시 용량, 두 구현 |
| `src/l6_marked_capacity_pruned_144.c` | 건전한 가지치기 두 개 (+ SHADOW 모형) |
| `src/l6_coupled_144.py` | 조각 모형 (끝점 표시 결합 DP) |
| `src/l6_chain_capacity_144.c` | 사슬·병합사슬 모형 + 증인 모드 |
| `src/l6_chain_rows_144.py`, `src/l6_build_*_144.py` | 사슬 모형 행 평가·표 생성 |
| `src/l6_rows_final_144.py` | **세 모형의 올바른 결합** |
| `src/l6_871_analysis_144.py` | 871 잔여 행 분석 |
| `src/l6_circuit_coexist_144.py` | 순수 순환 공존 시험 (exact cover) |
| `src/l6_coexist_check2/3_144.py` | 같은 시험의 독립 두 번째·세 번째 구현 |
| `src/l6_witness_check2_144.py` | 증인 열거 독립 재구현 (run 단위·강제 분할) |
| `src/l6_final_144.py` | 전 과정 재실행 + 판정 |
| `src/l6_certificate_144.py` | 인증서 (해시·노드 수·digest) |
| `tests/test_l6_endgame_144.py` | 회귀 시험 25 개 |
| `outputs/witness_144/` | 등호 증인 문자 보존 |
