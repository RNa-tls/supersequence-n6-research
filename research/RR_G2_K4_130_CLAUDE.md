# 라운드 130 — 일반 `(k,G) = (4,2)` 칸

**작성:** Claude 단독. **엔진 재작성 + 정확 탐색 라운드. 두 원장 수치 변경 없음.**
**라운드:** 130 (generic (k,G) = (4,2) cell)
**원장:** INDEPENDENTLY AUDITED **4,782** · CLAUDE FULL-JOINT Q2 **6,396/6,396** — 변경 없음.
**라벨:** ROUND-130 PROVISIONAL · Claude 단독 · **독립 감사 없음** (Codex 쿼터 소진).

`(3,2)` 로 넘어가지 않았고, `G ≥ 3` 을 시작하지 않았으며, 바깥 열 좌표로 `F` 를 쓰지 않았다.

---

## 0. 결과

> ### `(4,2)` 의 다섯 하위경우 중 **둘을 완전히 닫았다.  칸은 아직 닫히지 않았다.**
>
> | 하위경우 | 실행 | 완료 | `UNSAT_COMPLETE` | 캡 | SAT | 노드 | 초 | 상태 |
> |---|---|---|---|---|---|---|---|---|
> | **`A / e=0`** | 10 | **10** | **10** | 0 | 0 | 6,878,643,289 | 701 | **닫힘** |
> | **`B / e=0`** | 25 | **25** | **25** | 0 | 0 | 48,653,209,658 | 4,818 | **닫힘** |
> | `A / e=1` | 10 | 0 | — | — | — | — | — | **남음** |
> | `B / e=2` | 25 | 0 | — | — | — | — | — | **남음** |
> | `B / e=1` | 100 | 0 | — | — | — | — | — | **남음** |
> | **합** | **170** | **35** | **35** | **0** | **0** | **55,531,852,947** | **5,519** | |
>
> **지금까지 캡 도달 0, SAT 0.**  스윕은 계속 돌고 있고 재개 가능하며 결과를 증분으로
> 기록한다.  남은 세 하위경우는 **`UNKNOWN` 이 아니라 아직 실행되지 않은 것**이다 —
> 판정을 승격하지 않는다.
>
> ### 그러나 구조는 완전히 확정됐다
>
> * 다섯 하위경우를 라운드 129 표를 믿지 않고 **처음부터 다시 유도**했고 정확히 다섯이다.
>   모두 **`x = H = 0`, `F = 2`, `f_out = e + 2`, `S = 25`, `N = −1`, `L = 871`(등호)** 이다.
> * **유형 A / `F = 1` 은 `k = 4` 에 존재하지 않는다** (`k + x + H ≤ 2 + F = 3`).
> * `e = 0` 인 두 하위경우에서는 **어느 짧은 pass 가 자유로 나가는지까지 강제**된다:
>   자유 탈출은 전부 경우 (i) ⟹ `ν`-상승이고 `F = 2` 라 상승이 정확히 둘이기 때문이다.
>   유형 A 는 `arc0`·`arc1`, 유형 B 는 두 **opener** 다.  `A/e=1` 도 **경우 (ii) = `arc2`** 로
>   완전히 강제된다.
> * 그 강제와 **국소성 lock**(경우 (i) 의 자유 후속이 여는 run 이 `ν(p)` 를 담는다)이 실측으로
>   같은 실행을 **캡 도달 → 1.31×10⁹ → 6.81×10⁸** 으로 줄였다.
>
> ### 엔진은 라운드 125 를 **노드까지** 재현한다 — 그 대조가 버그를 잡았다
>
> `-DTARGET=121`, `mtype = 2` 로 빌드하면 세 대조가 **20,584 / 462,058 / 487,122** 로
> 전부 정확히 일치한다.  초판은 짧은 pass 에서 사용-육각형 마스크를 세우지 않아
> 462,255 로 어긋났고, **그 불건전성을 대조가 발견해 고쳤다.**
>
> ### 양성 대조: 거짓 기각 **0**
>
> `G = 2` 짧은 pass 기계가 합법 `n = 4` walk 의 **`G = 2` 10,625개를 전부** 받아들인다
> (유형 A `ν`-순서 1,138 · 역순환 46 · 유형 B 9,441).
>
> **칸이 닫히지 않았으므로 원장은 `9 / 55` 그대로다.**

---

## 1. 다섯 하위경우를 처음부터 다시 유도한다 (§1)

라운드 129 의 표를 **믿지 않고** 다시 센다.

    k = 4, G = 2  ⇒  P = 120 + G = **122**,  O = 24 + k = **28**,  D = 5k − G = **18**
    L = 869 + k + e + x + H − f_out ≤ 871,  k = 4  ⇒  **f_out ≥ e + x + H + 2**
    라운드 129 정리 129.1:  f_out ≤ F + e,  그리고 F ≤ G = 2
      ⇒  e + x + H + 2 ≤ f_out ≤ F + e ≤ 2 + e
      ⇒  **x = 0, H = 0, F = 2, f_out = e + 2**
    S = 23 + k + e + x − f_out = **25**,  N = S − 22 − k = **−1**,  r = 28 + e,  t = 1
    f_out ≤ #짧은 pass = G + m  ⇒  유형 A(3개) `e ≤ 1`,  유형 B(4개) `e ≤ 2`

| # | 유형 | 내부 `F` | `e` | `x` | `H` | `f_out` | `S` | `N` | `r` | `L` |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | A | 2 | 0 | 0 | 0 | 2 | 25 | −1 | 28 | 871 |
| 2 | A | 2 | 1 | 0 | 0 | 3 | 25 | −1 | 29 | 871 |
| 3 | B | 2 | 0 | 0 | 0 | 2 | 25 | −1 | 28 | 871 |
| 4 | B | 2 | 1 | 0 | 0 | 3 | 25 | −1 | 29 | 871 |
| 5 | B | 2 | 2 | 0 | 0 | 4 | 25 | −1 | 30 | 871 |

**정확히 다섯** — 라운드 129 와 일치한다.  모든 하위경우에서 `L = 871` 이 **등호**다.

## 2. 유형 A / `F = 1` 은 `k = 4` 에 존재하지 않는다 (§2)

정리 129.1 의 귀결 `k + x + H ≤ 2 + F` 에 `F = 1` 을 넣으면 `k + x + H ≤ 3` 이므로
`k = 4` 는 불가능하다.  전수 유도에서도 유형 A/`F=1` 의 기여는 **0** 이다.
따라서 남는 내부 모형은 **A/F=2 와 B/F=2** 둘뿐이다.

## 3. 자유 탈출 구조 (§3) — 그리고 그것이 **강제 패턴**이 된다

`f_out = e + 2` 이고 짧은 pass 는 3개(A) 또는 4개(B) 다.

| 하위경우 | 짧은 pass | 자유 | 비자유 |
|---|---|---|---|
| A / `e=0` | 3 | **2** | 1 |
| A / `e=1` | 3 | **3** | 0 |
| B / `e=0` | 4 | **2** | 2 |
| B / `e=1` | 4 | **3** | 1 |
| B / `e=2` | 4 | **4** | 0 |

### 3.1 `e = 0` 이면 어느 pass 가 자유인지까지 강제된다

`e = 0` 이면 **분열 궤도가 없다**.  자유 탈출이 경우 (ii) 이려면 목표 궤도에 run 이 둘
있어야 하므로, **모든 자유 탈출은 경우 (i)** 이고 경우 (i) 는 `p < ν(p)`, 즉 **`ν`-상승**이다.
`F = 2` 이므로 상승은 **정확히 둘**이다.

* **A / `e=0`** — walk 순서가 `arc0 < arc1 < arc2` (`F = 2` ⟺ `ν`-순서) 이므로 상승은
  `arc0`, `arc1` 이다.  ⟹ **`arc0`·`arc1` 이 자유이고 `arc2` 는 절대 자유일 수 없다.**
* **B / `e=0`** — 각 2-순환의 상승은 walk 에서 먼저 오는 pass, 즉 슬롯을 **여는** pass 다.
  ⟹ **두 opener 가 자유, 두 closer 는 비자유.**

### 3.2 유형 A / `e=1` 도 완전히 강제된다

`f_out = 3` 이라 세 arc 이 전부 자유다.  경우 (ii) 는 목표 궤도가 셋 다 다르고 분열 궤도가
하나뿐이라 **많아야 하나**이며, `#경우(ii) ≥ f_out − F = 1` 이므로 **정확히 하나**다.
경우 (i) 는 상승뿐이라 `{arc0, arc1}` ⟹ **경우 (ii) = `arc2`.**

### 3.3 `B / e=1` 과 `B / e=2` 는 강제되지 않는다 — 정직하게 나눈다

`B / e=1` 은 넷 중 셋이 자유인데 **어느 것이 비자유인지 강제되지 않으므로 네 갈래로
나눠** 각각 정확히 돌린다.  `B / e=2` 는 넷 다 자유지만 opener 가 경우 (ii) 일 수도 있어
**국소성 lock 이 증명되지 않는다** — 쓰지 않는다.

## 4·5. 국소성 lock — 경우 (i) 의 구조적 귀결 (§4, §5)

경우 (i) 인 자유 pass `p` 의 후속 `τ(entry(ν(p)))` 는 **`ν(p)` 를 담는 바로 그 run** 을 연다.
`τ` 가 궤도에 고정점 없이 작용하므로 `ν(p)` 는 그 run 의 첫 pass가 아니고, run 은 최대 5
pass 이므로

> **`p` 의 자유 탈출 직후부터 `ν(p)` 가 놓일 때까지 walk 은 그 궤도를 떠날 수 없고,
> `ν(p)` 는 5 pass 안에 나타난다.**

이 lock 을 **증명된 곳에만** 건다: `A/e=0`(arc0·arc1), `B/e=0`(두 opener),
`A/e=1`(arc0·arc1).  `B/e=1`·`B/e=2` 에는 걸지 않는다.

**실측 효과** (`A/e=0`, 분할 `(1,1,4)`, 캡 2×10⁹):
강제 패턴 없이 **캡 도달** → 강제 패턴만 **1.31×10⁹ 완주** → lock 까지 **6.81×10⁸ 완주**.

## 6·7. `H = 0` 과 `x = 0` 의 구조적 귀결 (§6, §7)

`H = 0` 이 **정확**하므로 이 칸에는 무게 4/5/6 이음매가 **존재할 수 없다** — 엔진은
`M4`·`M5`·`M6` 를 **어디에서도 제공하지 않는다** (`HW = 0`, `HCAP = 0`).
남는 것은 `ω ∈ {2,3}`, 즉 `W2`(자유) 와 `W3a`/`W3b`/`W3c` 넷뿐이다.

`x = 0` 이 **정확**하므로 **궤도 내부 비용-1 이음매가 하나도 없다.**  `ℓ = 5` 에서 궤도
내부인 비용-1 이동은 `W3a` 하나이므로, 실질적으로 **full pass 뒤의 `W3a` 가 전면 금지**된다
(짧은 pass 뒤의 `W3a` 는 궤도를 바꾸므로 inter-run 이고 허용된다).  엔진은 이를 분류에
의존하지 않고 `same && c == 1 ⟹ x++ > XCAP = 0` 로 **직접** 거부한다.

## 8·9. 엔진 (§8, §9)

`src/g2_cell_130.c` — 라운드 125 엔진의 **일반 부품은 그대로**, 짧은 pass 상태 기계만
새로 썼다.  `G = 1` 의 `sstate 0→1→2` 기계를 `G = 2` 에 억지로 끼우지 않았다.

| 그대로 재사용 | 새로 씀 |
|---|---|
| `build()` 기하 (`hexid`/`orbid`/`phse`/`SIG`/`hlo,hhi`) | `#define TARGET 122` |
| 경량 이동표 `M2`·`M3a`·`M3b`·`M3c` | 유형 A: **3-호 기계** (한 육각형, `ν` 순서) |
| `EXC` 궤도-덮개 초과 · `fod` 신선-궤도 결손 | 유형 B: **독립 2-호 슬롯 기계 둘** (서로 다른 육각형) |
| `NTAB`/`BESTSEG` · `capacity_ok`/`dcommitted`/`dfeasible` | `FREESPEC`/`FREEON` 강제 자유 패턴 |
| 캡/UNKNOWN 의미론 | `LOCKSPEC` 국소성 lock |

**유형 A 기계**: 세 호는 `(w, ℓ0)`, `(σ^{ℓ0}(w), ℓ1)`, `(σ^{ℓ0+ℓ1}(w), ℓ2)` 이고
`ℓ0+ℓ1+ℓ2 = 6`.  `F = 2` ⟺ 세 호가 walk 에서 **정확히 그 순서**로 나타나므로
(라운드 129), 기계는 `ν` 순서만 허용한다 (`AORDER = 1`).

**유형 B 기계**: 슬롯 둘.  각 슬롯은 **신선한 육각형**에서 `(v, b)` 로 열리고 짝
`(σ^b(v), 6−b)` 로 닫힌다.  끼워넣기를 허용한다.

**모든 pass 는 자기 육각형을 "진입됨" 으로 표시**하고(그래야 나중 full pass 나 새 슬롯이
그 육각형에 못 들어간다), **markhex 는 그 육각형의 마지막 호에서만** 부른다(그때 비로소
육각형이 완전히 소진되어 신선 궤도를 막기 때문).  이 두 가지를 분리하지 않은 초판은
**불건전**했고, 아래 §13 의 재현 대조가 그것을 잡아냈다.

## 10. 분할 모양 (§10)

* **유형 A**: `(ℓ0, ℓ1, ℓ2)` 순서 있는 분해 **10가지**.
* **유형 B**: `(b₁, b₂) ∈ {1..5}²` **25가지** (`b₁` 은 먼저 열리는 육각형의 분할).

정준화는 **증명된 대칭으로만** — `S₆` 좌곱으로 walk 의 첫 진입 단어를 고정하는 것뿐이다.
반전 `Φ` 접기는 이 칸에서 증명되지 않아 **하지 않았다** (§16).
`k = 4` 의 자원 제약은 어떤 분할도 죽이지 않으므로 35가지가 전부 남는다.

**최종 실행 수**: `A_e0` 10 + `A_e1` 10 + `B_e0` 25 + `B_e2` 25 + `B_e1` 25×4 = **170**.

## 11. 용량 경계 (§11)

`D = 18` 은 `G=1` 열의 `k=4`(`D=19`) 와 거의 같고 `(1,1)`(`D=4`) 보다 훨씬 느슨하다.
run 결손은 `5r − P = 5(28+e) − 122 = 18 + 5e` 라 조각 용량 프룬이 약하다 —
**`k = 4` 에서는 해석적으로 죽는 행이 하나도 없다**(라운드 129 와 일치).
예상대로 `D = 18` 만으로는 힘이 없고, 실제 절감은 §3–§5 의 강제 패턴과 lock 에서 온다.

## 13. 재생 대조 (§13)

새 엔진을 `-DTARGET=121` 과 `mtype = 2`(두 번 진입 육각형 **하나** = `G = 1`)로 빌드하면
라운드 125 엔진과 **노드까지 같아야 한다.**

| 경우 | 라운드 125 | 라운드 130 엔진 | 일치 |
|---|---|---|---|
| `e=0, f=0, rmax=26` | 20,584 | **20,584** | **예** |
| `e=1, f=0, rmax=26` | 462,058 | **462,058** | **예** |
| `e=1, f=1, rmax=26` | 487,122 | **487,122** | **예** |

> 이 대조가 **실제로 버그를 잡았다.**  초판은 짧은 pass 에서 사용-육각형 마스크를 세우지
> 않아 같은 육각형에 full pass 가 다시 들어갈 수 있었다.  대조가 462,255 ≠ 462,058 로
> 어긋난 덕분에 발견했고, 고친 뒤 **세 경우 모두 정확히 일치**한다.

라운드 129 의 구조 대조(유형 A·B 분류, 순환-회전 규칙)는 `tests/test_g2_cells_129.py` 가
그대로 유지한다.

## 14. 양성 대조 — 거짓 기각 0 (§14)

엔진의 짧은 pass 기계 **의미론**을 파이썬으로 다시 구현해
(`src/verify_g2_machine_130.py`) 합법 `n = 4` walk **29,255개** 중 **`G = 2` 인 10,625개
전부**에 적용했다.

| | 값 |
|---|---|
| `G = 2` walk | **10,625** |
| 기계가 받아들인 수 | **10,625** |
| **거짓 기각** | **0** |
| 유형 A · `ν` 순서 (`F = 2`) | 1,138 |
| 유형 A · 역순환 (`F = 1`) | 46 |
| 유형 B | 9,441 |

유형 A 기계는 `AORDER = 1` 에서 **정확히 `F = 2` walk 을** 받아들인다 — `(4,2)` 칸에서는
`F = 2` 가 강제되므로 그것이 맞는 설정이고, `F = 1` walk 은 같은 기계의 `AORDER = 0` 이
받아들인다.

## 15·16·17. 파일럿 · 그룹화 · 긴 실행 (§15, §16, §17)

### 파일럿 (실행 전 보고)

| 하위경우 | 분할 | 강제 없이 | 강제 패턴만 | 강제 + lock |
|---|---|---|---|---|
| `A / e=0` | `(1,1,4)` | 2×10⁹ **캡** | 1.31×10⁹ 완주 | **6.81×10⁸ 완주** |
| `A / e=1` | `(1,1,4)` | 2×10⁹ **캡** | 2×10⁹ **캡** | 4×10¹⁰ **캡** |
| `B / e=0` | `(1,1)` | — | — | **1.74×10⁹ 완주** |

처리량은 일정하게 약 **1.0–1.1×10⁷ 노드/초**.

### 그룹화 (§16)

분할 모양은 **수학적으로 동치일 때만** 묶었다 — 실제로는 하나도 묶지 않았다.
`S₆` 좌곱으로 첫 진입 단어를 고정하는 것만이 증명된 축약이고, 반전 `Φ` 접기는 이 칸에서
증명되지 않아 **쓰지 않았다**.  `B/e=1` 은 어느 pass 가 비자유인지 강제되지 않으므로
**네 갈래로 나눠 각각** 돌린다(추측으로 묶지 않는다).

### 긴 실행 계획 (실행 전 보고)

| 항목 | 값 |
|---|---|
| 최종 구조 하위경우 | **5** |
| 총 실행 수 | **170** (`A_e0` 10 · `A_e1` 10 · `B_e0` 25 · `B_e2` 25 · `B_e1` 100) |
| 해석적 폐쇄 | **0** (`k = 4` 에서는 조각 용량이 아무것도 죽이지 못한다) |
| 측정된 파일럿 노드 | 약 5.7×10¹⁰ (위 표) |
| 인스턴스당 캡 | **3×10¹⁰ 노드** (약 45분) |
| 동시 실행 | **1개** (순차, 재개 가능, 증분 기록) |

### 실제 진행

`A_e0`(10) 과 `B_e0`(25) 가 **완주**했다 — 35 실행, 5.55×10¹⁰ 노드, 92분, **캡 0 · SAT 0**.
`A_e1`·`B_e2`·`B_e1` 은 아직 실행되지 않았다.  `A_e1` 은 파일럿에서 4×10¹⁰ 노드로도
완주하지 않아 이 하위경우가 **지배적**이다.

## 18·19. UNKNOWN 과 SAT 처리 (§18, §19)

**캡에 걸린 실행이 하나도 없다.**  걸렸다면 `UNKNOWN` 으로 남기고 절대 승격하지 않는다.
아직 돌지 않은 세 하위경우는 `NOT_RUN` 으로 기록했고 **`UNSAT` 로도 `UNKNOWN` 으로도
읽지 않는다.**

**SAT 은 나오지 않았다.**  나왔다면 122-pass 전체 자취 · 구체적 단어 · 다중도 유형 ·
내부 `F` · 짧은 pass · `ν` 순환 · `e`/`x`/`f_out`/`H` · `k`,`G`,`D` · 길이를 보존하고
literal NR6 의미로 재생할 준비가 되어 있다 (엔진이 `witness_words`/`witness_lengths` 를 출력한다).

## 20. 덮개 행렬 (§20)

칸이 닫히려면 **다섯 하위경우 전부와 모든 분할 모양**이 해석적으로 불가능하거나
`UNSAT_COMPLETE` 여야 한다.  현재:

| 하위경우 | 분할 모양 | 덮인 것 | 남은 것 |
|---|---|---|---|
| `A / e=0` | 10 | **10 UNSAT_COMPLETE** | 0 |
| `B / e=0` | 25 | **25 UNSAT_COMPLETE** | 0 |
| `A / e=1` | 10 | 0 | **10** |
| `B / e=2` | 25 | 0 | **25** |
| `B / e=1` | 100 (25×4) | 0 | **100** |

⇒ **칸은 닫히지 않았다.**

---

## 21. 원장 (§21)

| 라벨 | 값 |
|---|---|
| **INDEPENDENTLY AUDITED (Q2 잔여)** | **4,782** — 변경 없음 |
| **CLAUDE FULL-JOINT Q2** | **6,396 / 6,396** — 변경 없음 |
| 바깥 표 | **55칸, 지표 `(k, G)`** |
| **Claude 가 닫은 칸** | **9 / 55** — **늘리지 않았다** (칸이 닫히지 않았으므로) |
| `(k,G) = (4,2)` | **부분** — 다섯 하위경우 중 둘 완주, 셋 미실행 |

NR6 는 여전히 **가정**이다.  `(3,2)` 로 가지 않았고 `G ≥ 3` 을 시작하지 않았다.

## 22. 다음 라운드에 남기는 것

1. **`A / e=1` 이 지배적이다** (파일럿이 4×10¹⁰ 노드에서도 미완주).  구조적 여지가 남아
   있다: 그 하위경우에서는 분열 궤도가 **`orb(entry(arc0))` 으로 특정**되고 단 하나의 궤도
   재방문 사건이 **`arc2` 의 자유 탈출 바로 그 지점**으로 강제된다.  이를 프룬으로 넣으면
   큰 절감이 기대되지만 이 라운드에서는 구현하지 않았다(추측이지 결과가 아니다).
2. `B / e=1` 은 실행 수가 100개로 가장 많지만 각각은 더 강하게 제약된다.
3. `B / e=2` 는 lock 이 증명되지 않아 강제가 약하다 — opener 가 경우 (ii) 일 수 있는지
   따로 따져 보면 lock 을 쓸 수 있을지 모른다.

**This project has not proved `L₆ ≥ 872`.**
